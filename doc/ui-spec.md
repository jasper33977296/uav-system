# UI 完整規格書（全頁面：配色／顯示內容／動作邏輯／文字圖）

- 狀態：**已核准並實作完成**（核准：使用者 2026-08-11 含六點修訂＋
  事件列改位；實作：批 1–2c＋改位全數落地並通過驗收 2026-08-11）。
  **§2.5 編隊模式已核准（使用者 2026-08-11），可實作**。
  §7 未答項採預設：HUD 不加飛行時間、機隊頁保留「飛行中」chip、
  回放速度選項收 ⋯。遺留兩小項：任務改名（等後端 rename API，低優先）、
  回放頁點圖表跳時刻（下批，時間軸拖曳已涵蓋需求）。
- 涵蓋：全域規則＋五頁（即時／機隊／路徑／回放／比較）＋導覽
- 已實作：即時頁、機隊頁、比較頁（審核中發現要改的照本文件修）；
  未實作：路徑頁縮圖卡、回放頁抽屜（本文件核准後動工）

---

## 0. 全域規則

### 0.1 配色總表（深色＝預設）

| 用途 | 色值 | 說明 |
|---|---|---|
| 頁面／地圖底 | `#1b1a17` | 暖炭黑，全站畫布 |
| 卡片／面板底 | `#262624` | 浮動卡、抽屜 |
| hover／chip 底 | `#30302c` | 第三層 |
| 主文字 | `#f0eee6` | 暖白 |
| 次文字 | `#c2bfb3` | 輔助數字、說明 |
| 弱文字 | `#8f8b80` | 時間戳、單位 |
| 分隔線 | `#3a3833` | 髮線、格線 |
| 強調（互動限定） | `#d97757` 珊瑚 | **只准**：導覽當前頁、起飛鈕、非危險主要按鈕。禁入：地圖、圖表、狀態元件 |
| 危險／緊急 | `#e05e5e` 紅 | 返航鈕、上鎖、降落、danger 浮出句底 |
| 警告 | `#fab219` 黃 | warn 浮出句、warn 事件點 |
| 正常 | `#0ca30c` 綠 | ok 浮出句、連線亮點 |
| 訊號分級（軌跡＋訊號格） | 良好 `#0ca30c`／尚可 `#fab219`／劣化 `#e05e0e`／瀕斷 `#a01818` | 全站唯一一套「狀態」色 |
| 航線識別（比較頁多線） | 1 `#3987e5` 藍／2 `#d95926` 橘／3 `#199e70` 綠 | 「身分」色，與狀態色不互用；>3 條退灰 `#8f8b80` |
| 機隊識別（多機） | **三槽**依連線順序：`#3987e5` `#d95926` `#199e70`（僅此三槽通過暖畫布 CVD all-pairs 驗證）；**第 4 台起一律 `#8f8b80` 灰**——顏色到此停止承載識別 | 機體球、地面投影線、機隊頁色點、編隊成員點 |
| **多機識別的硬規則**（2026-08-12 裁定，起因：實測第 4/5 台外觀完全相同、且 `droneLayer.ts` 註解宣稱的「常駐 sysid chip」在 UI 中**根本不存在**——選中機統一那批移除後註解未更新，形成**紙上緩解**） | **凡顯示機隊色點之處，一律同時顯示機身文字標籤**（sysid 或機名縮寫），不分機數多寡：3 台以內文字是冗餘保險、超過 3 台文字是唯一識別依據。**不擴充色盤**（加色需重跑 CVD all-pairs，且第 N 台問題永遠存在，加色只是把牆往後推）。**3D 球體同樣適用**——球體目前連 hover 都沒有，是最嚴重的一處。 | 依據硬約束「顏色永不單獨傳達語意」 |
| 計畫路徑（編隊） | 單機＝灰（現況不變）；編隊＝各機識別色 40% 淡化細帶＋同色地面虛線——計畫是「該機的未來」，同色系但不與實跡的狀態色混淆 | 即時頁多計畫路徑 |

淺色主題為手動 opt-in（值見 design-tokens.md），本文件以深色為準。

### 0.2 全站互動原則

| 原則 | 內容 |
|---|---|
| 危險操作兩段式 | 第一次點→按鈕變紅並顯示「確定？」，3.5s 內再點才執行 |
| 緊急操作單擊 | 返航／懸停：點一下立即執行（緊急時多一步是風險） |
| 地圖縮放 | Ctrl＋滾輪（單滾輪＝捲頁面）；雙擊＝放大；拖曳＝平移／旋轉 |
| 摺疊記憶 | 工作區資訊（5G 詳細、回放圖表、CDF）記住展開狀態；教學文不記 |
| 異常 toast | 同時多事只顯示最嚴重一則；**一律顯示 10 秒或使用者點擊即消除**（使用者定案 2026-08-11）。持續性危險在 toast 消失後仍由 HUD 呈現（訊號格 0 格斜線、失聯樣式），事件流留紀錄——toast 是通知，不是狀態的家 |
| 空狀態 | 沒有的資訊不畫（不放「—」）；無資料時一句現況＋灰 icon |

### 0.2b 三種「非正常」不得互相冒充（2026-08-12 裁定，混機情境）

混機時同一畫面會同時出現三種非正常狀態，**視覺與語意必須三分，不得
共用樣式**——它們對操作者的意義完全不同：

| 狀態 | 意義 | 視覺 | 文案 |
|---|---|---|---|
| **未就緒** | 知道不行（有明確原因） | 黃點 `--status-warn` ＋原因行 | 「未就緒」＋ not_ready_reasons 逐條 |
| **遙測不足** | **不知道**（判斷依據本身缺席） | **灰空心點**（`--muted`、空心＝無斷言）| 「**尚無足夠遙測**」——不寫「就緒」也不寫「不可飛」 |
| **未驗證鎖定** | 協定未驗過，刻意保護 | warn 黃底橫幅＋機型徽章 | 「此機型控制尚未驗證，指令已鎖定」（既有 §2.5）|
| （對照）故障 | 壞了 | 紅 `--status-danger` | 錯誤原文 |

**判定規則**：`ready=true` **但其依據欄位（prearm_ok／ekf_ok）皆為 null**
時，**不得顯示綠燈「就緒」**——那是**沒有依據的布林值蓋過一整排空白**，
屬「空值繞過」的變體（前例：§0.1 徽章顯「—」等於形同移除）。此時一律
走「遙測不足」灰空心態，直到依據欄位有值。

**為什麼不能沿用紅色**：「不知道」與「壞了」若同色，操作者會把一台
剛連上、還沒送齊遙測的正常機當成故障機處置——**誤判方向相反的錯誤，
比不顯示更糟**。同理「未驗證」是我方刻意保護、非機體異常，用 warn 不
用 danger。

### 0.2e-2 機體的三種「沒有資料」不得同形（2026-08-24，issues/036）

同一個症狀「機顯示在即時頁上但沒東西」底下是三種不同的事，處置也不同：

| 情況 | 呈現 | 理由 |
|---|---|---|
| **曾連上、現在斷線** | 保留在畫面上顯示**最後已知位置**，加**紅色外框閃爍**動畫 | 找回失聯機時最後位置最有用；但**必須看得出它已經斷線**（使用者定案） |
| **從未連上** | **完全不出現在即時頁** | 沒有「最後已知」可言。註冊清單走無人機頁，那是另一條路徑 |
| **連著但沒有位置估計** | 機體不畫在地圖上，其餘遙測（高度、姿態、電量）照常顯示 | 沒有位置就是沒有位置，不是位於 0,0 |

**兩個實作上的陷阱，都已踩過：**

1. **`0,0` 是自駕儀的「不知道」哨兵，不是座標。** `GLOBAL_POSITION_INT` 在沒有
   位置估計時送 lat=lon=0。ingest 若照寫，會把 `float | None` 這個誠實的型別
   覆蓋成假座標，而地圖的 `!= null` 過濾器**擋不住 0**——機體被畫在幾內亞灣外海。
   判準要用哨兵值而不是 GPS fix：位置來源不一定是 GPS（室內光流、動捕）。

2. **斷線動畫必須由本地時鐘驅動。** 機一斷線遙測就停了＝store 不再變動＝畫面
   不重繪，於是「已經斷線」這件事會因為斷線本身而顯示不出來。這是 §0.2g
   那條通則的又一實例：**故障的偵測不得依賴故障的那條路徑**。

### 0.2e 兩種「灰」不得同形：沒量到 vs 有量測但沒有這個欄位（2026-08-13）

分級軌跡在無 SINR 時退回灰線。但**灰線目前同時承載兩種完全不同的
狀況**，而它們在畫面上一模一樣（實測：欄位名由 `sinr` 改為 `snr`，
灰線像素數與「真的沒量到」完全相同）：

| 情況 | 成因 | 目前呈現 | 應有呈現 |
|---|---|---|---|
| 完全沒有 link 樣本 | 這趟沒收訊號資料 | 灰線 | 灰線＋「這趟沒有訊號量測」 |
| **有 link 樣本，但 SINR 全為 null** | **上游改欄位／我方對映失效** | 灰線（同上，無從分辨） | 灰線＋「**有 N 筆量測，但沒有 SINR 值**」 |
| 部分樣本無 SINR | 量測斷續 | 該段灰 | 該段灰（不補值，維持現況） |

**判定極廉價且確定**：`link.length > 0 && sinr 非 null 數 === 0`。
一個布林運算就把**看不見的失效換成看得見的陳述**。

**為何必須分**：第二種幾乎一定是缺陷（有人送了量測卻沒有這個欄位），
但它偽裝成第一種——**一個合法、常見、不會有人追問的狀態**。使用者
只會記下「這趟沒訊號資料」而繼續研究，**上游欄位改名可以就這樣活過
整個實驗**。這是 §0.2b 的同族：**失效不得穿上合法狀態的外衣**；
差別只在這裡冒充的不是另一種非正常，而是**一種正常**——更難察覺。

**通則**：凡「缺值即退回中性呈現」的地方，都要能回答
**「是沒有這筆資料，還是這筆資料裡沒有這個欄位？」**——答不出來，
該處就存在一個永遠不會被回報的失效。

**同族第三種：我方取得失敗被說成「沒有資料」。** `fetch` **不會**因
HTTP 5xx 而 reject，而錯誤回應的 body（`{"detail": …}`）是合法 JSON——
`d.link ?? []` 於是得到空陣列、流程視為成功，畫面說出「這趟沒有訊號
量測」。**後端掛掉時，即時頁會宣告飛機一切正常。** 取用點一律先看
`r.ok` 再 `json()`，失敗說「無法取得…」而非「沒有…」。

**「兩種狀態塞進同一句括號，等於兩個都沒宣告」**（回放頁實例：
「載入軌跡中…（若架次無樣本則無可回放）」——載入卡住時看起來像沒
樣本，沒樣本時看起來像還在載）。**每個狀態各自有話，才叫有宣告。**

**這類缺陷與注意力無關**：§0.2e 的修正在同一個 commit 裡就犯了 §0.2e
要防的錯（`.catch()` 接不到 5xx）。**防線只能是被測試釘住的陳述，
不能是「記得要注意」。**

**兩道防線分工不同，缺一不可**：**型別/介面防初次犯**（呼叫端根本拿
不到「靜默變成空陣列」這個選項），**測試防回歸**（有人改回去就紅）。
測試只覆蓋想得到的失敗，所以不能當唯一防線；反之沒有測試釘住，
介面日後會被繞過。

**同句混態實測清單（2026-08-13 掃全庫）**：已修——回放頁「載入軌跡中…
（若架次無樣本則無可回放）」、比對回放頁「載入中，或此路徑尚無已完成
的航線」、比較頁「載入兩趟軌跡中…」；**未修——機隊頁
`app/drones/page.tsx:309`「讀取中，或 backend 未連線」**（同一形狀第四
例）。**此形狀比 `r.ok` 更常見**，因為它不需要任何錯誤發生就已經是
錯的：句子本身就拒絕宣告。

### 0.2f 系統必須能陳述自己是哪個版本（2026-08-13，事故調查暴露）

**現況（已查證）**：`apps/frontend/Dockerfile` 是 `COPY . . && npm run build`，
**全 repo 的 Dockerfile 與 compose 皆無任何版本注入**（無 git sha、無 build id、
無 LABEL）。部署環境**無法回答「我是哪個 commit」**。

**後果**：2026-08-13 真機事故調查中，「事故環境帶有哪些已知缺陷」只能靠
**bundle 內容指紋考古**（UI 字串出現/消失夾區間；連字串都沒變的修正還得比
minify 後的語句順序）。**每一次事故都要重做一次考古**，而考古的每一步都可能
把判準套錯對象——本輪已實際發生兩次（見 §0.2e 註）。

**要求**：
1. **建置時注入 git sha**（build arg → 環境變數），**工作區不乾淨時標記 `-dirty`**。
2. **UI 可見**：設定／關於處顯示 build id；不佔用飛行畫面版面。
3. **端點可讀**：提供版本端點，讓調查與自動檢查不必開瀏覽器。
4. **`-dirty` 或缺版本一律顯示為「未知版本」**，不得留空——
   §0.2e 同一條：**「不知道」要說出來，不能靠畫面上沒東西表達**。

**為何這屬 UI/UX 而非只是維運**：操作者與調查者是**同一群人**，而
**一個無法陳述自身狀態的系統，等於要求使用者用考古來取得本該直接顯示的事實**。
這與我們整輪在修的是同一件事——**把看不見的事實換成看得見的陳述**。

### 0.2g 畫面必須能證明自己還活著（2026-08-14，分支 A 實測定案）

**實測發現的洞**：socket 開著但幀停止（後端凍住、或無 FIN 的網路中斷——
真機 TCP keepalive 以小時計）時，**45 秒零告警、畫面逐像素不變**。
成因：`ageStale` 讀的是**後端送來的** `link_age_s`（幀停了它就凍住不再成長）、
`wsConnected` 只在 socket **關閉**時才翻——**兩條偵測都依賴故障的那條路徑**。

**通則**：**故障的偵測不得依賴故障的那條路徑**（已移除的 deadman 當初刻意走
前端本地時戳，是同一條原則的另一個實例）。**靜態畫面與凍結畫面在視覺上無法區分**，所以畫面上必須有
**由本地時鐘驅動、持續變化的元素**——**一個會自己變的數字，是畫面還活著的
唯一證明**。

#### F1 本地幀活性（新增）

前端**收幀時記本地時戳**，與後端欄位無關。WS 推送實測 **5 Hz（`broadcast_hz=5.0`，
`.env` 無覆寫）＝正常間隔 200 ms**，門檻據此定：

| 階段 | 門檻 | 呈現 | 文案 |
|---|---|---|---|
| 常駐計時 | **≥ 2 s**（10 幀未到） | **次要文字色**，一行 | 「更新於 N 秒前」（**N 持續跳動**） |
| 同一行升級 | **≥ 5 s** | 同一行轉 **warn 色**，不新增元素 | 同上 |
| 橫幅告警 | **≥ 10 s**（50 幀未到） | **err 橫幅** | 「**系統已 N 秒沒有送資料過來——畫面停在中斷前，無人機現況未知**」 |

**為何是 2／5／10**：正常間隔 200 ms，2 s＝10 幀未到已非抖動；**5 s 沿用系統
既有的「不新鮮」時間感**（機端 `connected` 翻 false、`ageStale` 皆為 5 s）——
**同一個時間感讓使用者只學一次**；10 s＝50 幀，不可能是暫時性，才升橫幅。
**5 s 只換色不換元素**是刻意的：一次 5 秒的後端停頓不該噴紅色橫幅，
**常駐的無害警報會擠掉真警報**（噪音預算）。

**與既有三條告警不重疊**：那三條宣告的是「**收到的資料告訴我們什麼**」
（ws 斷／機失聯／資料舊）；F1 宣告的是「**我們什麼都沒收到**」——
更根本的一層，且在幀停止時**只有它會亮**。

**這型失效有前例**：後端廣播迴圈曾因 `datetime` 混進 `json.dumps` 而死，
**幀停送、日誌乾淨**（backend docstring 有記）。即使三條告警設計正確，
**廣播路徑自己死掉時前端照樣全盲**——F1 是唯一能防這型的東西。

**文案三句各有職責**：說我方發生什麼（系統沒送資料，**段別正確**——不是機、
不是機的鏈路）／說畫面現在是什麼（停在中斷前）／**說我們不知道什麼**
（無人機現況未知）。**不得寫成「無人機失聯」**——那是對機體的斷言，我們沒有依據。

**背景分頁**：瀏覽器會節流背景分頁計時。**常駐計時照實顯示**（回到前景時
數字跳大是誠實的），**但橫幅只在前景觸發**——不在使用者看不到的時候喊，
也不因切分頁而誤報。

**已知限制（要寫進註解）**：本地計時只能證明「頁面的 JS 還在跑」，
**不涵蓋整個分頁被瀏覽器凍結**；但分頁恢復時計數會跳，仍會留下痕跡。

#### F2 持續狀態必須有持續呈現（一般化）

`ageStale`（後端回報的機→後端逾時）目前**只有 10 秒 toast，之後畫面無痕跡**，
而該狀態可能持續數分鐘。**toast 只能表達「發生了一件事」，不能表達
「正在持續的狀態」**——事件用 toast，狀態用常駐呈現。

| 狀態 | 持續呈現 | 形狀 |
|---|---|---|
| 正常 | 訊號格實心 | — |
| **`ageStale`** | **訊號格虛線空心＋顯示 `link_age_s` 秒數** | 虛線 |
| `droneLost` | 訊號格斜線（既有） | 斜線 |

**三者形狀互異**（§0.1「顏色永不單獨傳達語意」；§0.2b「不得互相冒充」）。
F1 的本地停更走**另一個位置**（計時行／橫幅），與 F2 的訊號格不同軸，
因此兩種「不新鮮」不會被混為一談——**它們的成因不同段，處置也不同**。

### 0.2d `mode_verb`：跨廠牌模式語意的用法（2026-08-12）

後端提供 `mode_verb`（`hold`／`mission`／`rtl`／`land`／`position`，
未知或解不出＝`null`）作為**廠牌無關的模式語意**，與顯示用的
`flight_mode` 並存。實測：PX4 `HOLD`→`hold`、ArduPilot `LOITER`→`hold`。

**用法三條（顯示與判斷分離）**：

1. **顯示層永遠用 `flight_mode` 原廠模式名**——那是機端真的在跑的模式，
   照「機上原文不翻譯」原則。**`mode_verb` 不得取代顯示**。
2. **判斷層一律用 `mode_verb`**：狀態判定、事件摺疊、跨機一致性檢查、
   任何「這台機在做什麼」的程式判斷。**禁止比對 `flight_mode` 字串**
   ——PX4 `HOLD` 與 ArduPilot `LOITER` 是同一件事，比字串必在混機時錯。
3. **混機時的人話註記**：≥2 種 autopilot 同時在線時，模式名旁加語意
   括注（`LOITER（定點停懸）`）——**單一廠牌時不加**（無歧義就不加字，
   簡約優先）。`mode_verb=null` 時**不加註記也不猜**。
   **顯示位置僅兩處：任務控制狀態列、事件流 mode_change 句。**
   （**IMU 卡無模式列**——§2.6 定案時已將「就緒／模式／GPS」自抽屜刪除，
   理由是與面板狀態列重複；本節初稿誤列第三處，2026-08-12 更正。
   **記取**：規格引用其他章節的既有元素時須查證其現況，我方文件本身
   也會過時——這與程式註解留在舊世界是同一種失真。）

### 0.2c 「受限」態的呈現（2026-08-12 裁定）

> **起因是 ArduPilot 搖桿需機端 `SYSID_MYGCS=254`。該功能已於 2026-08-24 移除
> （[issue 035](../issues/035-remove-manual-control.md)），但本節規則不隨之作廢**——
> 「能力存在但有前提」的情境仍在（如 `mission_start`／`mission_fly` 的未驗證態），
> 而下方條款 4、5 是通則。搖桿相關的舉例保留為歷史案例。

四態中「受限」＝**能力存在，但有使用者可自行滿足的前提**（對照：
「未驗證」＝我方尚未驗證、使用者無從自行解除）。三點定案：

1. **按鈕可按，不禁用。** 理由：前提在**機端**、我方無法探知是否已滿足
   （設好了也不會通知我們）。禁用等於替使用者判定「你沒設」——那是
   我方不知道的事。禁用還會讓已正確設定的使用者**永遠無法使用該功能**。
2. **視覺區別（不可混淆）**：
   | 態 | 語意 | 按鈕 | 標記 |
   |---|---|---|---|
   | 受限 | **可以按，但你要先做一件事** | **可按** | 鈕旁 `ⓘ` 弱色＋常駐前提句（一行，非橫幅） |
   | 未驗證 | **現在不准按** | **禁用** | warn 黃底橫幅（既有 §2.5）|
   受限**不用 warn 黃底**——黃底是「我方擋下」的語言，受限沒有擋。
3. **前提句常駐、不藏 tooltip**：例「需在機上設定 `SYSID_MYGCS=254`，
   否則指令會被忽略」。**因為失敗是靜默的**（指令被丟棄、無錯誤回傳），
   使用者沒有其他線索可循——這是唯一會告訴他真相的地方。
4. **靜默失敗的補償（關鍵）**：受限能力按下後，若**逾時未見預期狀態
   變化**（如搖桿送出但 flight_mode 未進手動、無姿態回應），
   **視同失敗並顯示該前提句**——不得永遠停在「送出中」。
   **原則：能力標記為受限時，UI 必須有能力偵測它的失敗**；否則
   受限與 ok 對使用者而言無異，標記形同虛設。

5. **偵測不到失敗時，必須降級為「未驗證」**（2026-08-12 實例）：
   ArduPilot 搖桿經後端協定分析——`MANUAL_CONTROL` 是**串流式輸入、
   MAVLink 協定上無 ACK**，機端信不信我方 sysid，我方看到的完全一樣；
   間接觀測（推桿後姿態變化）又受限於「只有飛行中可測、風擾與位置
   保持會誤判、且判定時機正好是操作員推桿沒反應的最危險那幾秒」。
   故**標 `unverified`（鎖住）而非 `limited`（可按但永遠不告訴你失敗）**。
   能力提示需同時寫明「為什麼鎖著」與「怎麼解」。

5b. **每廠牌數值限制必須是 UI 可讀的契約，不得是內部常數**
   （2026-08-12，起因：起飛高度預設 `10.0` 全域寫死不分廠牌）。
   若某廠牌最低起飛高度為 15 m，UI 上的「10」看起來與任何正常數字
   無異，使用者無任何線索——**我方替使用者做了一個我方沒有依據的
   判定**（同 §0.2c 形狀）。規則：**輸入欄位的預設值／最小值／最大值
   凡因機型而異者，一律由後端 `limits()` 隨 capability 提供，UI 不寫死**
   （同 `retention_days` 手法：單一事實源，顯示值與實際約束不可能不一致）。
   缺 limits 時：**不套用任何預設數字**，顯示佔位提示要求使用者輸入，
   而非假裝我方知道安全值。

6. **優於「事後偵測失敗」的作法：事前讀機端參數確認前提**（後端提出、
   採納為未來方向）。連上時讀 `SYSID_MYGCS`：等於 254 才把該能力開為
   `ok`，否則維持鎖住並顯示**具體事實**「機端 SYSID_MYGCS=255，需改為
   254」。**這比 limited 更符合誠實原則**——給的是可行動的事實，而非
   讓使用者按下去等一個靜默失敗。**通則：前提可事前查證時，就不要
   讓使用者用失敗去發現它。**

### 0.3 異常浮出句（全站文案，禁術語）

| 觸發 | 句子 | 級 |
|---|---|---|
| 與後端 WS 斷線 | 與系統失去連線——畫面可能不是最新 | danger |
| 機 connected=false | 無人機失聯——顯示的是最後已知位置 | danger |
| ↑ 地圖上同時以**紅色外框閃爍**標示該機（2026-08-24，issues/036）。排在機體圖示**之下**不遮蔽本體；透明度起伏但不歸零——閃到全透明會有半個週期看不出異常，而那正是這個標示要防的事。 | |
| link_age_s > 5s | 資料延遲——畫面可能不是最新 | warn |
| ~~deadman 觸發~~ | ~~操控中斷——無人機已自動懸停~~ | **已移除（035）** |
| ↑ 這一列與虛擬搖桿一起移除，但**它留下的通則必須保留**（2026-08-12 註，源於 issue 030）：ArduPilot 的失聯降級曾誤切 GUIDED 而非 LOITER——**畫面說「已自動懸停」，機體實際進入的卻是可被外部接管的模式**。**通則：UI 文案宣告了機體行為時，該行為的正確性就是文案的前提**——後端行為變更若使文案失真，屬 UI 回歸，需同步修文案或行為。 | |
| failsafe 危急 | **無人機進入緊急狀態：{是哪一種}——正在自動處置** | danger |
| SINR 瀕斷 | 訊號快斷了 | danger |
| SINR 劣化 | 訊號變差了 | warn |
| 訊號恢復 | 訊號恢復了 | ok |
| GPS 劣化 | 衛星訊號變弱——位置可能不準 | warn |
| 指令被拒 | **無法{動作}：{後端給的那句話}** | warn |
| 未就緒（還沒按） | **現在還不能飛：{第一條原因}（另有 N 項）** | warn |
| 自動返航（等 origin 欄位） | 無人機自動返航中 | warn |

### 0.3a 通知要說出**問題點**，不是只說「有問題」（2026-09-02 現場實測）

現場情況：操作員在試飛，地面站一直不給控。同一時間有**三件不同的事**擋著
——油門桿沒推到底、飛控的 GCS failsafe、入列守門未通過——而畫面上那一句是

> 現在還不能起飛——點這裡看原因

**三件事一視同仁。** 要知道是哪一件，得自己展開面板、或去翻事件流，
而人正站在場邊拿著遙控器。

**而原因後端一直都有給**：`not_ready_reasons` 帶著是哪一項預檢
（`ArduPilot 預檢未過：Throttle (RC3) is not neutral`）、指令被拒的
`detail.msg` 帶著為什麼。**那些字是在前端被丟掉的。**

三條規則：

1. **句子要帶那一格事實。** 「無法解鎖：這台機沒有連線中的機上代理」
   勝過「指令被拒」，因為前者說得出下一步。
2. **每一次被拒都要浮出來，不只起飛。** 原本只有 `/takeoff` 會通知 HUD，
   於是按解鎖／切模式被 403 擋下時，面板收起來的人什麼都看不到。
3. **不必等按下去才說。** `ready === false` 時就把第一條原因浮出來
   ——「為什麼不能飛」在按之前就已經知道了。

長度：toast 截到 64 字並保留展開（點擊開面板看全文）。
**一句讀不完的通知，與一句沒有內容的通知一樣沒用。**

### 0.3a-2 說得出「怎麼辦」，不只「哪裡不對」（2026-09-02 使用者要求）

> 使用者原話：**「我以後要知道這個具體原因是啥，以及怎滿足解鎖的條件」**

飛控說的是**現象**，不是**動作**：`Throttle (RC3) is not neutral` 說得出哪裡
不對，說不出要做什麼。而人站在場邊，需要的是後者。

同一趟裡連續遇到三種擋法（油門桿位置、GUIDED 不接受搖桿解鎖、電池 failsafe），
**每一種的處置完全不同**，而畫面上三者長得一樣。

對照表在 `apps/frontend/lib/prearm.ts`，三條紀律：

1. **原文不刪。** 處置是**加**在原文後面的一行，不是取代它——翻譯會失真，
   而查韌體行為時要的是飛控原本那句話。
2. **認不得就不要編。** 比不到樣式就明說「這一項還沒有對應的處置」。
   **看起來合理但其實錯誤的指示，比沒有指示危險得多**——這條規矩本專案在
   PX4 事件翻譯上已經立過一次（版本不符時寧可不給 text）。
3. **處置只給一行**（2026-09-07 使用者回報）。原本一條電池 failsafe 掛的是
   「換一顆充飽的電池；這一項在地面會擋解鎖，但在天上要 `BATT_FS_*_ACT`
   不是 0 才會有動作」——而未就緒時螢幕上同時站著四五條原因，每條再掛兩行
   就是一整片沒有人會讀的字，**連「換一顆電池」那幾個字都被埋掉**。
   補充說明（`armNote`）改掛 tooltip：知識不丟，但不佔版面。

> 使用者原話：**「只需要點出問題是電池電壓低於門檻就好」**——
> 處置行回答的是「我現在要做什麼」，不是「這個參數怎麼運作」。

toast 也帶處置（截到 96 字），因為那正是站在場邊的人要的那半。

### 0.3b 畫面上不出現「這句話的出處」（2026-09-02 使用者裁定）

**使用者不想知道一句話源自哪個 issue，也看不懂。** 所以 UI 文案裡不得出現：

* issue 編號與裁定代號——`（039 複裁 A）`、`（詳 issues/015）`、`#004`
* 文件章節——`§2.8`、`ui-spec §0.2e`、`狀態機文件 §3-A`
* 內部命名——`backend`、`min_samples`、`登錄表`、`payload`、服務或模組名

**規則不是「不准解釋」，是「解釋要用他能做的事來說」。** 兩者的差別：

> ✗ 遙控器未連線——自動起飛的前提是有人能隨時接管，請先確認遙控器開機並與飛控連上**（039 複裁 A）**
> ✓ 遙控器未連線——自動起飛的前提是有人能隨時接管，請先確認遙控器開機並與飛控連上

**前半句留著**：它說的是「為什麼擋你」與「怎麼解掉」，那是操作員要的。
刪掉的只有出處——那一格對他而言是雜訊，**而且它會讓整句話看起來像除錯訊息，
於是連前半句也被跳過**。

> **出處寫在程式註解裡，不是寫在畫面上。** 本專案的註解是主要的決策檔案，
> 那些引用要留著——**它們的讀者是下一個改這段程式的人，不是飛手**。

### 0.3c 畫面文案不得帶 Markdown 記號（2026-09-07）

**本站沒有任何一處在渲染 Markdown。** 於是文案裡的 `**強調**` 會原樣長成
星號印在畫面上：

> ✗ 電池電壓低於門檻。換一顆充飽的電池；`**`這一項在地面會擋解鎖⋯`**`
> ✓ 電池電壓低於門檻，換一顆充飽的

註解與本文件照常用 `**`（那是給改程式的人看的）；**進到畫面的字串與 JSX
文字節點一律不用**——要加重就用 `<b>`。

同一類的排版陷阱：**JSX 文字節點裡的換行會塌成一個空格**，中文標點後面
因此多出一道縫（「本系統沒上傳過，␣或是別的 GCS 傳的」）。整句不折行，
或把折點挪到元素／表達式邊界（那裡的換行會被 JSX 去掉）。

---

## 1. 導覽列（全站共用）

```
┌──────────────────────────────────────────────┐
│ UAV 監控  [✛] [✕] [〜] [∿]          ⬤ 記錄中 │
│  品牌字    即時 機隊 路線 比較    唯一燈號(醒目)│
└──────────────────────────────────────────────┘
```

- 配色：底 `#262624`、icon 未選 `#c2bfb3`、當前頁 `#d97757`＋淡珊瑚底。
- 顯示：品牌字；四個 icon（✛ 定位十字＝即時、四旋翼剪影＝機隊、
  三節點折線＝路線、雙折線疊影＝比較）。
- **狀態燈只留一顆：記錄燈**（使用者定案 2026-08-11，後端／資料流
  兩顆移除）。要醒目：12px 圓點＋文字「記錄中」，錄製中＝紅點呼吸燈
  ＋紅字；未記錄＝灰點＋「未記錄」灰字。後端/資料流異常已由
  異常 toast 與 HUD 失聯樣式涵蓋，不需燈號。
- 動作：點 icon 切頁；記錄燈無互動（語意在 title：armed 且入庫中）。

---

## 2. 即時監控頁（/）

### 2.1 文字圖——正常飛行

```
┌──────────────────────────────────────────────┐
│ 導覽列                                        │
│              [▤] [地圖|影像] ┌任務控制———▾┐   │ ← 右上：面板本體（可收合div）
│                             │● 就緒  任務… │   │    地圖切換在其左側
│              （全幅 3D 地圖） │[起飛][返航]… │   │
│                             └─────────────┘   │
│           航跡絲帶（訊號分級色）                │
│           機體球＋航向箭頭（識別色）             │
│ ┌圖例────────┐                               │
│ │● 良好(≥13dB)│                               │
│ │● 尚可(5–13) │                               │
│ │● 劣化(-2–5) │                               │
│ │● 瀕斷(<-2)  │                               │
│ │─ 預計任務路徑│                               │
│ │○ 起飛點     │                     50 m ▭   │ ← 比例尺（右下）
│ └───────────┘                               │
│ ▲30m  →4.2  ▮▮▮▯  🔋81%                    │ ← HUD 在圖例下方，互不重疊
└──────────────────────────────────────────────┘
```

（事件面板位置＝使用者二次修訂 2026-08-11：**併入 ▤ 抽屜、
置於專業數值卡下方**；三次修訂：**移除單行收合/展開切換，事件列表
常駐展開、高度自動填滿欄內剩餘空間**（flex-grow，列表內部捲動，
無固定 vh 上限），critical 紅底照舊。右欄平時只有任務控制面板。）

- **任務控制面板＝自身可收合的浮動 div**（使用者定案：不用另外的
  ⌃ 按鈕藏它）：初始位置**畫面右上角**（導覽列下方），點標題列收合成
  一條（「任務控制 · 就緒」），可拖曳、位置記憶（沿用原 CommandPanel
  機制）。主按鈕（起飛/返航）併入面板首行，HUD 右側不再放按鈕。
- **面板永遠留在畫面內**（2026-09-07 使用者回報：面板超出頁面範圍，
  連拖都拖不動）。**這是一條硬規則，不是排版偏好**——飄到畫面外的
  控制面板等於**指令通道在畫面上消失**，而它裝的是返航與降落。三件事：
  1. **高度上限**＝面板頂端到視口底（逐次量，不寫死：面板在流內是
     `map-wrap` 座標、拖走後是視口 `fixed`，兩者可用高度不同）；
     超過就**面板內部捲動**。
  2. **標題列 sticky**：那是唯一的拖曳把手，捲丟了就等於面板搬不動。
  3. **座標回收**：視窗縮放、或面板自己長高（未就緒原因一次冒五條）
     之後，重算存在 localStorage 的位置——那個座標是上次視窗尺寸算的。
     拖曳進行中不回收（使用者的手正在決定的事，不跟他搶）。
- **[地圖|影像] 切換在面板左側**、▤ 抽屜鈕再左。
- **軌跡顏色圖例回到左下角常駐**（使用者定案：舊左下顯示是好的），
  內容：四分級＋預計任務路徑＋起飛點；擺位在 HUD 上方、間距 10px，
  與 HUD／事件列／比例尺互不重疊（1366×768 驗證）。圖例從抽屜的
  訊號品質卡移除（單一住所：圖例的家＝地圖左下）。

### 2.2 文字圖——異常時（toast 通知）

```
│         ┌──────────────────────┐             │
│         │ ⚠ 訊號快斷了       ✕ │             │ ← 導覽列下方中央 toast
│         └──────────────────────┘             │    10 秒自動消失或點擊即消
```

**位置規範（使用者反饋 2026-08-12 對正）**：toast **以整個視窗水平置中**
（`left:50% + translateX(-50%)`，非地圖區置中、非任何面板的相對定位），
垂直在導覽列下方 12px；右上三件套與右欄不影響其定位（toast 短暫且
層級最高，寧可蓋住也不偏移）。

### 2.3 顯示內容

| 元素 | 內容 | 資料源 | 何時顯示 |
|---|---|---|---|
| HUD ▲ | 相對高度（m，整數） | WS alt_rel | 恆顯 |
| HUD → | 地速（m/s，1 位小數，無單位字） | WS ground_speed | 恆顯 |
| HUD 訊號格 | 4 格階梯條；分級→格數 4/3/2/1，失聯 0 格＋斜線；格色＝分級色 | WS sinr→classify | 恆顯 |
| HUD 電池 | 膠囊圖形填充＝電量＋百分比數字；<20% 填充轉紅 | WS battery_pct | 恆顯 |
| 任務控制面板 | 右上浮動可收合 div：首行＝就緒點＋主按鈕（地面「↑ 起飛」珊瑚／空中「⌂ 返航」紅）；展開＝任務選擇/上傳/起飛→任務/解鎖/降落。**任務區沒有起飛高度欄位**——見 §2.3a | 多源 | 恆顯，**預設收合**（使用者定案 2026-08-11：收合列高 32px 與 [▤][地圖|影像] 同高對齊，就緒點＋主按鈕留在收合列） |
| 軌跡顏色圖例 | 四分級＋預計任務路徑＋起飛點（左下常駐卡） | 靜態 | 恆顯 |
| 事件卡 | 抽屜內、專業數值卡下方：**常駐展開列表，高度填滿欄內剩餘空間**（內部捲動，無收合切換） | events | 開抽屜時 |
| 異常 toast | §0.3 表；10s 或點擊消除 | 多源 | 異常時 |
| 機隊色點（左上） | 每機一色點，選中者外圈 | WS fleet | ≥2 機時 |
| 抽屜 ▤ | 自上而下：「訊號品質」卡（SINR hero＋分級 chip＋走勢圖＋RSRP/RTT/丟包＋「詳細」摺疊 5G 欄）→「**IMU**」卡（§2.6，原專業數值卡改造）→「事件」卡（單行最新＋展開 log）——圖例在地圖左下不在卡內 | WS | 點 ▤ |
| 檢視切換 | 地圖 ↔ 選中機即時影像 | video_url | 恆顯 |

### 2.3a 任務起飛高度沒有欄位（2026-09-07 使用者指示）

「起飛→任務」的離地高度**跟著任務自己的 `NAV_TAKEOFF` 走**，畫面上
**不提供覆寫欄位**。

沿革：原本這格預設 10 m／下限 3 m，於是一份 takeoff 2 m、航點 3 m 的
低空航線會先被拉到 10 m 才切任務——**實際飛行高度是規劃的三倍以上，
而那個 10 不在任何一份 `.plan` 裡**。當天先改成「留空＝跟任務」；同日
使用者裁定整格移除：**一個永遠應該留空的欄位不該出現在畫面上**——它問
的是一個已經有答案的問題，而填錯它就讓飛機不照那份航線飛。

後端契約不變：`POST /mission/fly` 的 `takeoff_alt` 仍是選填（rig、MCP、
curl 這些呼叫端要指定得了），只是**這個畫面不送它**。

### 2.3b 任務控制面板的按鈕（2026-09-07 使用者指示）

* **同一列的按鈕等寬等高。** 標籤長短不一時，一排大小不一的鈕讀起來像
  三種不同分量的操作，而它們是同一層級的東西。實作是 `flex: 1 1 0`
  ＋ `align-items: stretch`——**平分整列，不是照字數分**。
  兩段式確認尤其需要：標籤從「清除任務」變成「確認清除任務？」時寬度不動
  （同列一起變高），按鈕不會在你要按第二下的那一瞬間跳位。
* **一次只做一件事。** 任何一個指令還在等回覆時，面板上其餘會「送東西
  出去」的按鈕一律按不下去——連按兩顆的結果是兩條指令在鏈路上交錯，
  而它們的先後順序沒有人保證得了。
  界線畫在**會不會送出去**：取消提案、關掉通知、退出編隊、返回設定只改
  畫面狀態，鎖住它們只會讓人在等待的那幾秒裡連退路都沒有；編隊入口例外地
  鎖住，因為它換掉的是指令的對象。編隊的「中止」不受這條規則管——那是
  緊急出口。
  > **已知取捨**：`btn()` 一律受這條規則管，所以返航／降落在另一條指令
  > 等回覆期間也按不下去，最長是指令服務的 30 秒逾時。這是既有行為，
  > 本次只是把規則寫明並補上原本沒鎖的幾顆。要不要把緊急鈕排除在外，
  > 是一個待裁定的問題。
* **按鈕上不放編號。** 「① 上傳」「② 開始任務」那組序號是實作順序的
  說明，不是使用者要讀的東西——順序已經由左到右的排列講完了。

### 2.3c 緊急原地降落（2026-09-07 使用者指示）

> 使用者原話：**「隨時都可以按不會被其他規則管，是整個系統優先權最高的
> 指令，用於出現意外情況時使用」**

**住在任務控制面板的標題列，永不 disable，單擊即發。** 面板收合著也在——
緊急時不該先展開才找得到它。實心紅底：面板上其餘的鈕都是外框或淡底，
只有這一顆是實心，任何狀態下它都是視線最先落到的東西。

送 `POST /api/command/{sysid}/emergency/land`（切 LAND、原地下降）。

**它跳過什麼**

* 地面站的所有節流：等回覆的鎖（它不共用 `busy`）、兩段式確認、面板收合。
  那些是防手滑與防指令交錯用的，而這一顆存在的理由正是
  「其他東西卡住的時候它還要能按」。
* **機上守門（第三層）**。守門回答的是「當下狀態允不允許」，而「把飛機
  放下來」在任何狀態下的答案都一樣——RTL／LAND 在意圖通道斷線時本來
  就已經是這樣處理（`admission.OFFLINE_ACTIONS`）。

**它不跳過什麼——這三道不是流程，是「送出去會不會做錯事」**

| 門 | 為什麼留著 |
|---|---|
| `ENABLE_COMMANDS=false` | 那台部署宣告過自己只觀察不指揮。繞過一個顯式宣告的安全開關，不會讓飛機更安全 |
| 入列 | 身分不明的機不是我們的機——對它下降落指令可能是在指揮別人的飛機 |
| 能力（`land` 非 `ok`）| 機型未驗證時我方不確定 LAND 在它上面對應到哪個模式，送過去可能切到別的東西 |

三道都說得出是哪一道擋的，理由同時進結果行與 HUD toast。**擋下的當下，
操作員手上還有實體遙控器——那才是最後一道，不是這一顆。**

留痕：`command_log` 的 action 是 `emergency_land`，與一般的 `mode:land`
分得開——回放一趟飛行時，「有沒有人按過緊急降落」是最想知道的事之一。

### 2.3d 上傳成功之後還要說的話：地形預檢（2026-09-07，issues/047 §1-B）

> 起因：`lowspeed-test-260907` 那一趟。航點是 `frame 3`（相對**起飛點**），
> 地面沿路抬高多少就吃掉多少離地空間，而飛控不會抱怨——它忠實地維持
> 「離起飛點 1 m」，然後撞上去。

**上傳成功不等於沒事。** 結果行在「上傳成功」底下附地形預檢的話：

```
✓ 上傳成功
  ⚠ seq 3→5 之間（離 seq 3 約 120 m）：地面比起飛點高 8.0 m，預期離地只有 0.6 m
  ⚠ 地形資料是 SRTM（水平約 30 m、只有地形不含樹木電線）⋯
```

**只挑地形那幾句**（`check.terrain.notes`），不是整個 `warnings`：那裡還有
圍欄、機種、frame 方言的話，全列會變成沒人讀的一大段（使用者：字太多）。

三條顯示規則：

1. **「沒有地形資料」要說出來，不能不說。** 查不到高程與「離地很夠」在
   資料上長得一樣（都沒有 problem）——沉默會被讀成通過。
2. **餘裕不足要分得出是不是地形造成的。** 「地面高了 8 m 吃掉餘裕」與
   「航線本身就規劃在 1 m」的處置完全不同（改航線繞開 vs 你要用比資料
   誤差還小的餘裕飛），混成同一句會讓真正的地形警告被當成雜訊。
3. **解析度的誠實話跟著發現走**，不是每次都念一遍：有東西可報的時候，
   操作員才需要知道這份資料撐不撐得住他要做的決定。

**擋門**：預期離地是負的（航線穿過地面）→ 409，**預設就擋**
（`TERRAIN_ENFORCE`，與 `GEOFENCE_ENFORCE` 分開——圍欄擋下來多半是
系統預設值跟場地無關的假錯誤，地形擋下來是摔機的形狀）。被擋時一定
附合法做法：差多少公尺、可改地形跟隨、以及「SRTM 在有樹的地方量到的是
樹冠，確定是假警報就關掉開關」。

### 2.4 動作邏輯

| 操作 | 行為 |
|---|---|
| 點機體球 | 開該機即時影像視窗（未設定→空狀態＋「去設定」鈕） |
| 點訊號格 | 開抽屜並定位到訊號品質卡 |
| 抽屜事件卡 | 常駐展開；列表內捲動；來源篩選 chips、DEBUG 摺疊——事件批。**點事件列＝開事件詳情 modal（§2.7）** |
| 起飛鈕 | 兩段式：變紅「確定起飛？」→再點執行 |
| 返航鈕 | 單擊立即執行 |
| 面板標題列 | 點擊＝收合/展開；拖曳＝移動（位置記憶）；危險鈕兩段式照舊 |
| toast（起飛被拒） | 點擊展開原因列表（not_ready_reasons＋最近 arming 拒絕）；其餘 toast 點擊即消 |
| 多機 | 點左上色點切換選中機；HUD／影像／控制跟隨選中機 |

### 2.7 事件詳情 modal（使用者核准 2026-08-12——可實作）

#### 文字圖

```
┌ 事件詳情 ────────────────────── ✕ ┐
│ 訊號瀕斷              ● 危急        │ ← 類型人話＋severity 點+標籤
│ 2026-08-11 17:42:31.284           │ ← 精確到毫秒
│ sim-uav-1（sysid 1）· 系統事件     │ ← 機＋來源
│ ─────────────────────────────────│
│ 內容                               │
│   sinr      -15.0 dB              │ ← detail 鍵值表（主呈現）
│   threshold < -2 dB               │
│ ▸ 原始 JSON                        │ ← 摺疊展開（加工與原始並存）
│ 重複 ×3　17:42:31 – 17:42:35      │ ← 折疊事件才顯示（次數＋時間範圍）
│                        [複製全文]  │
└───────────────────────────────────┘
```

#### 規則

0b. **PX4 事件字典的呈現（014 落地，2026-08-13 裁定）**

   實測資料形狀（同一批事件內即混合）：
   ```
   有字典：event_name=check_modes_manual_control, text="No manual control input", dict_fw=1.14.3
   無字典：event_name=null, text=null, dict_fw=null（只有 event_id 與 args）
   ```

   **(a) 英文原文不翻譯成中文——維持既有原則。** 與 `LOITER（定點停懸）`
   的差別在**可窮舉性**：模式名是少量固定集合，PX4 事件文字有數百條且
   **隨韌體版本變動**——自建中文表＝**複製上游知識並保證漂移**（§0.2d
   同一原則：語意只能有單一來源）。且操作者同時使用 QGC（亦為英文），
   **術語一致比翻譯更有用**；翻錯比不翻更糟。
   **例外**：`event_name` 若對應到我們**已有中文語彙的既有事件**
   （如 `navigator_rtl_home_activated` ↔ 既有「自動返航」），可比照
   §0.2d 加括注——但**靠 event_name 對應、不靠 text 字串比對**。

   **(b) 混合狀態是誠實行為，但不得讓「無翻譯」看起來次要。**
   有 text → 顯示原文；無 text → 「機上事件 #<id>」（既有）。
   **兩者的 severity 樣式、排序、可點擊性完全相同**——
   **缺翻譯是我方字典的限制，不是事件本身不重要**。
   若讓無翻譯者視覺上退居次要，等於用我方的能力缺口去評價機上事件的
   重要性，那是把限制偽裝成判斷。

   **(c) `dict_fw` 版本比對旗標——三態，各有不同呈現（2026-08-13 定案）。**
   `dict_fw` 本身顯示於 modal（列表不顯示，避免噪音）。比對旗標為
   `match` / `mismatch` / `unknown`（後端定義；因唯讀邊界不放行萬用指令
   載體，機上版本改由 command 服務落 DB，落地前**恆為 unknown**）。

   | 旗標 | 呈現位置 | 文案 | 色 |
   |---|---|---|---|
   | `match` | 不呈現 | — | — |
   | `mismatch` | **逐事件**（列 `⚠`＋modal 全句） | 版本不符，翻譯可能不準 | warn 黃 |
   | `unknown` | **每個視圖各一次**（見下） | 未能確認字典版本與機上韌體相符 | 次要文字色 |

   **`unknown` 的去重單位是「視圖」不是「頁面」**（2026-08-13 修正）：
   標記要出現在**使用者實際讀到那句翻譯的地方**，且在該視圖內只出現一次。
   - **清單視圖**（即時頁事件卡）→ 標頭一次。
   - **modal 視圖** → modal 內一次。modal 是 `position:fixed; inset:0` 全屏
     遮罩（45% 黑），**開啟時標頭那句被蓋住**——「面板層已標過」在此不成立。
   - **回放頁**（無事件清單，只有時間軸三角標）→ **不新增頁面層位置**，
     由 modal 內那次涵蓋。三角標開的是同一顆 EventModal，指涉完整。

   **判定範圍＝「帶 `dict_fw_match` 的事件」**：未經字典翻譯的事件
   （statustext、系統事件）**不參與全體判定**——否則事件流裡混進一筆
   系統訊息就會把面板態誤判成混合態，逐事件標記憑空出現。
   範圍亦隨來源篩選（全部／機上訊息／系統）連動：**判定的是使用者當下
   讀得到的那份清單**，與視圖去重同一條原則。

   **形狀分離**：mismatch 用 `⚠`＋warn 色，unknown 用 `?`＋次要文字色。
   兩者同列並排時，**形狀與顏色雙重可辨**（§0.1「顏色永不單獨傳達語意」），
   讀得出「這筆可能不準」與「這筆未確認」的差別。

   **不可重複標記**：標頭與逐事件標記由同一判定反向共用——`unknown`
   為全體時只出現在標頭，非全體時只出現在事件列，**結構上不可能兩者
   同時出現**。

   **混合態回歸逐事件**：當清單中 `unknown` **非全體**（版本路徑落地後，
   多機各自韌體不同即會發生：A 機 match、B 機 unknown），`unknown` 改為
   **逐事件標記**。理由是本條原始論證的反面——面板層去重的前提是
   **零區辨力**；一旦它能區辨哪幾筆未確認，全域一句反而**暗示全部未確認**，
   把局部缺口誇大成全體缺口。**逐項標記保留給逐項相異的性質**，這條在
   兩個方向上都要成立。

   **為何 unknown 現階段不逐事件標**：unknown 現階段**恆真**，逐事件標記等於
   在每一則上重複同一句話——**零區辨力的標記是純噪音，而噪音會訓練
   使用者忽略它**；等 mismatch 真正出現時，那個提示已經失去被看見的
   能力。**逐項標記應保留給逐項相異的性質**；unknown 是**系統狀態**
   （整個字典 vs 整台機的版本關係），標在面板層一次，**資訊量完全相同、
   噪音降為零**。版本路徑落地後出現 match/mismatch 混合時，逐事件標記
   自然只落在 mismatch 上，區辨力回復——**UI 屆時不需改動**。

   **為何 unknown 不用警告色**：unknown 不是異常，是**我方尚未具備的
   能力**；塗成警告色會讓使用者以為系統出了問題——那是另一種誤導。
   同 §0.2b 的形狀：**「不知道」不得冒充「有問題」**。兩句文案的主詞
   也刻意不同——unknown 說的是**我方未能確認**，mismatch 說的是
   **版本確實不符**。

   **為何不等 mismatch 出現才提示**：無法確認相符時就顯示翻譯而不聲明，
   等於**默認它正確**——unknown 不得被當成 match。此為 (c) 原始阻斷
   要求的完整應用（後端 2026-08-13 指出，成立）。

1. **內容依事件型別**：STATUSTEXT＝原句全文（不翻譯）；vehicle_event＝
   event_id＋args（hex）＋已知解譯（A.2 metadata 全文落地後同欄自動
   帶人話全文——**018 分層定案：列表＝摘要人話、modal＝細節層**）；
   系統事件＝detail 展開成鍵值表＋raw JSON 摺疊（誠實：加工顯示與
   原始並存，raw 不預設展開不嚇新手）。
2. **複製全文**：一鍵複製 plain-text 序列化（時間/機/來源/severity/
   型別/內容/raw）——除錯貼給別人用；複製後鈕短暫變「已複製」。
3. 開關：點事件列開（單一 modal，新點替換）；✕／Esc／點遮罩關。
4. 適用範圍：即時頁抽屜事件列表＋回放頁事件三角（點擊開同一 modal
   元件，取代現有 tooltip 的細節職責；tooltip 保留 hover 摘要）。
5. 列表 detail 若有截斷，modal 以單筆事件 API 取全文（後端側 PM 已
   同步）。

### 2.8 機上資料（Vehicle Data Phase 1：Inspector＋感測健康——
使用者核准 2026-08-12，可實作；排序在多機環境與 IMU/事件收尾之後）

#### 抽屜卡（緊湊入口，排事件卡上方）

```
┌ 機上資料 ────────────────────┐
│ 感測 ●陀螺 ●加速 ●磁力 ●氣壓 ✗GPS │ ← SYS_STATUS 健康位：健康綠點、
│ 訊息 14 型別 · 共 58 Hz [完整檢視] │    不健康紅＋名（缺位不畫）
└──────────────────────────────┘
```

#### 完整檢視（右側全高 sheet；✕/Esc/遮罩關）

```
┌ 機上資料 — sim-uav-1 ─────────── ✕ ┐
│ [搜尋訊息型別…]                     │
│ 型別               Hz     齡        │
│ ATTITUDE           9.8    0.1s   ▸ │ ← 預設按 Hz 降冪
│ SYS_STATUS         1.0    0.2s   ▾ │ ← 點列展開欄位
│   voltage_battery   12450  mV      │ ← 鍵值等寬；欄位名/單位照
│   drop_rate_comm    0.1    %       │    方言原樣（誠實，不翻譯）
│ #410               2.0    0.3s     │ ← 未知型別顯示 id，不隱藏
│ GLOBAL_POSITION…   4.9    6.2s ▒▒  │ ← 齡 >5s 整列淡化（資料舊了）
└────────────────────────────────────┘
```

#### 規則

1. 資料源＝後端訊息登錄表（1–2Hz 廣播：型別/頻率/最新欄位值）——
   畫面標注「更新率 1–2Hz」於 sheet 底部弱字（不是即時封包流，誠實）。
2. 多機：跟隨左上選中機（sheet 標題帶機名）。
3. 搜尋＝型別名/編號子字串過濾；排序 Hz 降冪固定（除錯情境的自然序）。
4. 三層定位（與既有卡分工）：儀表看 IMU 卡、log 看事件卡、
   **原始看這裡**——三者不互相重複欄位。
5. Phase 2/3（參數快照唯讀、ulog 回收）另案規格，不入本期。

### 2.6 IMU 卡（使用者核准 2026-08-11：專業數值改名 IMU、盡量全顯、
含高度三家安置——可實作）

#### 文字圖

```
┌ IMU ────────────────────────────┐
│ 姿態   Roll 0.3°  Pitch -9.9°   │
│        Yaw 251°（航向）          │
│ 角速率  0.1   -0.2    0.0  °/s  │
│         x      y      z         │ ← 三軸欄標一次（弱字），下列共用
│ 加速度 -0.05   0.21  -9.81 m/s² │
│ 陀螺    0.002  0.011  0.000 rad/s│
│ 磁力   21.3   -4.2   44.1  µT   │
│ 溫度   42.1 °C                  │
│ 振動   ▂▄▁ 12.4 ┃30 ┃60        │ ← 唯一圖形：帶門檻刻度的量條
│        clipping 0 / 0 / 0       │
│ 氣壓   1013.2 hPa（氣壓高度 488m）│ ← 契約有才畫
│ 導航估計  垂直速度 -0.0 m/s      │
│          海拔 488.0 m           │ ← alt_msl（原本無家，入此）
│ 位置   47.397746, 8.545596  ⧉   │ ← 選中機座標（等寬・點擊複製）
│        相對高度 30.2 m           │
└─────────────────────────────────┘
```

#### 編排原則

1. **三軸一律純數字**（等寬對齊、x/y/z 弱字欄標、單位弱字）——迷你
   bar 需要發明滿刻度，對原始感測值是造假比例；數字才誠實。唯一例外
   **振動量條**：PX4 有權威門檻（30/60），有意義的刻度才畫圖形，
   條上標門檻線，超標段染 warn/danger 色＋clipping 三軸計數。
2. **缺欄不畫**（feature-detect 慣例，照 §0.2 空狀態原則）：後端契約
   給哪些欄畫哪些列，缺整列不佔位、不放「—」。
3. **非 IMU 項的安置（單一住所，資訊不消失）**：
   - 機型 chip（PX4）→ 任務控制面板**對象行**機名旁（身分歸身分）；
   - 就緒／模式 chips → 已在面板狀態列，抽屜重複**刪除**；
   - GPS → 已在面板狀態列（GPS 3·10顆），抽屜重複**刪除**；
   - 航向 → 併入姿態列 **Yaw（航向）**——航向本就是 yaw；
   - 垂直速度 → IMU 卡末「導航估計」列（括注來源語意，不冒充原始
     感測值——誠實原則）。
5. **選中機座標**（使用者指示 2026-08-12：簡約重整時被收掉、需要回來）：
   IMU 卡末「位置」列，**等寬字型、6 位小數、逗號分隔、點擊複製**
   （現場抄座標是真實場景，複製後短暫顯「已複製」）。**放這裡而非
   HUD**：座標是「查詢時才要」的精確值，非常駐掃視項（HUD 只放
   飛行中一眼要看的四項）。**與地圖右下的游標經緯度不衝突**——那是
   「你指的位置」，這是「機在的位置」，語意不同、兩者並存。
   無座標時**整列不畫**（feature-detect 慣例）。
   **相對高度在此列重複顯示**（HUD 已有）——例外理由：抄座標時通常
   連高度一起抄，兩個數字分處畫面兩端會抄錯；此處為「查詢用的完整
   位置」，與 HUD 的「掃視用高度」用途不同。

4. **高度的三個家**：相對高度＝HUD（常駐快讀）；
   海拔（alt_msl，原本無家）＝導航估計列；氣壓計原始值＋氣壓高度
   （契約有才畫）＝感測列。

### 2.4b 底圖與機體圖示（017 P3 實施稿——大方向早經核准，本節為實施；
待使用者核准）

#### 底圖（NLSC 正射影像，opt-in）

- **入口**：地圖左下**圖例卡內最後一列**加一個切換（`底圖 [無|影像]`）——
  不新增浮動按鈕（簡約：圖例本就是「地圖上有什麼」的說明處，切換
  底圖屬同類），切換狀態 localStorage 記憶。
- **預設關**（深色畫布）：離線保證與對比基準不變（原定案）。
- **暗化 overlay**：影像層之上加暖色暗化（起始 50%，**實測定值**——
  以四段分級色在最亮地物（水泥/屋頂）上仍可辨為準）。
- **CVD 重驗：已執行，結論＝暗化提高至 80%，阻斷解除**（UI/UX
  2026-08-12，取樣自 NLSC 台北 z17 真實正射影像磚）：

  | 取樣 | 原色 | 暗化 50%（原定） | 暗化 80%（定案） |
  |---|---|---|---|
  | 最亮地物（屋頂/水泥） | `#c7c3bf` | `#716f6b` | `#3d3c39` |
  | 最暗地物（陰影/水體） | `#323d51` | `#272b34` | — |

  **50% 不足**：最亮地物上四色全部 <3:1（良好 1.49／尚可 2.73／
  劣化 1.38／瀕斷 1.59）。**80% 後僅瀕斷 `#a01818` 未過（1.39）**——
  **而它在現行無底圖畫布 `#1b1a17` 上本來就是 1.79，早已依既有 relief
  處置**（事件流卡片＋側欄數值分級標籤＋圖表事件標記，顏色從不是唯一
  訊號）。故**未新增失效模式**，阻斷解除。
  **CVD 分離與正常視覺分辨全程 PASS**（worst adjacent ΔE 11.3 protan／
  19.1 normal），色盤不動——符合定案「調暗化濃度而非改色盤」。
  **Lightness band 的 FAIL 不適用**：該檢查針對「類別色盤」，本組是
  狀態分級色（刻意有亮度階梯以承載嚴重度序），非同級可辨性問題。
  **觀感確認（數字過了不等於達成目的）**：暗化 80% 後於台北座標實截——
  街廓、建物輪廓、道路網、公園綠地皆清楚可辨，是「壓暗的正射影像」
  而非一片色塊，**底圖的空間定位功能成立**；同畫面四台機的識別色與
  機名標籤在影像上皆可辨。**驗證通過與目的達成須分開確認，本項兩者
  皆過，P3 收官。**
- **離線退回誠實顯示**（原定案）：瓦片抓不到時自動回深色畫布，
  並於圖例列標「**底圖離線**」——不留白畫布假裝使用者關掉了。

#### 機體圖示（2D 四旋翼俯視）

- 取代現行圓點/球體的**俯視四旋翼剪影**：機身＋四臂四旋翼，**隨
  heading 旋轉**（heading 已有；缺值時**不旋轉並停用方向語意**——
  不畫一個永遠指北的假朝向）。
- **著機隊識別色**（§0.1 三槽＋灰），**文字標籤規則不變**（一律配
  機名、同位錯開）。
- **影像背景上的可辨性**：圖示加 1.5px 暗色描邊＋輕微陰影（僅圖示，
  不加在軌跡上——軌跡靠暗化 overlay 保對比）。
- **3D 球體換模型：不做**（PM 定案）——2D＋底圖先上線看效果再議。

#### 回放頁的機體表示（2026-08-12 裁定）

**回放頁改用同一套 2D 四旋翼圖示、隨 heading 旋轉**（與即時頁一致）。
關鍵事實：**回放 track 有 heading 且全程有值**（實測 117/117 筆、
0–360° 完整範圍）——「回放無朝向資料」的前提不成立，故「維持球體」
的理由（朝向用不上）隨之消失。

**規格層的語意區分**（本可成為維持球體的理由，但不適用）：即時頁圖示
＝「機現在在哪、朝哪」；回放頁圖示＝「**你拖到的這個時刻，機在哪、
朝哪**」——後者是時間游標，但它指向的仍是**當時真實的機體姿態**，
不是抽象位置標記。既然朝向是真實記錄的資料，就該畫出來：**能還原的
事實不因它是「回放」而降級呈現**。

**配套**：回放頁圖示需與軌跡在視覺上可分（游標比軌跡點大、加白色外圈
或同等強調），避免使用者把游標誤讀為「軌跡上的一個點」。

**底圖切換：回放頁一併支援**（與即時頁同一入口位置與三態），
理由同上——回放是研究判讀的主場景，空間定位需求不低於即時。

#### 三項實作裁定（2026-08-12）

1. **`billboard:false` 貼地平面——維持，不改 billboard:true。** 傾斜
   視角下造型被壓扁是代價，但 `billboard:true` 會讓**機頭方向不再對應
   地面方位＝朝向淪為裝飾**。**朝向是飛行語意，造型是美觀**——衝突時
   語意優先（同「顏色永不單獨傳達語意」的精神）。
   **緩解（非改 billboard）**：圖示尺寸隨俯角補償（pitch 越大、
   圖示短軸放大係數越高，抵銷透視壓縮），以「俯視時正常、傾斜時仍
   看得出四旋翼輪廓」為驗收標準；仍不足時加大整體尺寸而非改朝向語意。
2. **「此區無影像」第三態——保留**（前端補位、核可）。NLSC 僅涵蓋
   臺灣，境外回 200 但為空白磚——**若只有「底圖離線」一種說法，
   使用者在境外開底圖會看到「開了卻沒變化」而以為壞掉**。三態各自
   對應不同事實：`底圖：影像`（正常）／`底圖離線`（抓不到瓦片）／
   **`此區無影像`（服務不涵蓋此範圍）**。措辭定為「**此區無影像
   （圖資僅涵蓋臺灣）**」——說明為什麼，而非只說沒有。
3. **CVD 重驗的執行條件**：模擬場域在瑞士、無真實地物可取樣，
   **重驗須待臺灣場域實地或以台北座標取樣**。**在此之前底圖功能
   仍可上線**（預設關閉、opt-in），但**規格標注「影像背景之色盤對比
   未驗證」直到取樣完成**——不因未驗證而卡住功能，但也不假裝驗過。

### 2.4c 地圖圖層五項修正（使用者 2026-08-12 反饋，待核准）

**驗證方式**：注入 60 點爬升＋轉彎軌跡（SINR 16→-8 全程跨四分級），
放大實截。以下皆為畫面實況，非推測。

| # | 問題 | 實況 | 定案 |
|---|---|---|---|
| 1 | **地面投影不見** | 藍色地面投影**還在**（`trail-line` 圖層），但**極細（1–3.5px zoom interpolate）且被空中絲帶壓過**，遠看等同消失 | **加粗並拉開視覺層次**：地面投影提高至 `2–5px`；維持機別色、維持虛化（它是投影不是實跡）。**空中絲帶與地面投影必須在任何 zoom 都能一眼分辨** |
| 2 | **圖示不該有箭頭** | 圖示帶機頭指標 | **移除機頭指標**——使用者裁定：**對無人機而言哪邊是頭不重要**，方向資訊由軌跡本身承載。圖示改為**對稱四旋翼**（原 heading 缺值時的造型），**heading 不再驅動旋轉**。§2.4b 的「隨 heading 旋轉」作廢 |
| 3 | **圖示被軌跡覆蓋** | 絲帶畫在圖示之上，機體被壓住 | **圖示永遠在最上層**（硬規則）：機體圖示與其標籤的繪製順序高於所有軌跡/投影/計畫路徑層。**理由：找不到機在哪＝監控失效**，任何美觀考量都不得凌駕 |

**⚠️ 層序的真正機制（2026-08-12 追查，比上表 #3 更根本）**：地圖混用
maplibre 與 deck（`MapboxOverlay(interleaved)`），**overlay 插在掛上
當下的最上層**——**任何在它之後 `addLayer` 的 maplibre 圖層都會無聲
蓋掉整個 deck**（含軌跡、圖示、標籤）。

- **實例三處**（皆已修）：即時頁與回放頁的計畫路徑（在 overlay 後建）、
  場域頁的網格/起飛點（在**另一個 effect** 裡等資料才建——**讀單一
  段落看不出問題**，這處最隱蔽）。
- **這使 #3 那條「已驗收通過」的硬規則，先前只在無啟用航線時成立**
  ——測試時 plan source 是空的，所以沒撞到。**一條規則若只在特定
  資料狀態下成立，它給的是虛假的保障。**
- **可執行的檢查**：**新增任何 maplibre 圖層前，必須確認其 `addLayer`
  在 overlay 掛上之前**；需等資料才能建的圖層，**先建空 source/layer，
  取得資料後 `setData`**。**症狀是「圖示不見了」，與新增的圖層看似
  無關**——這是最難聯想的失效模式，故列為明文檢查而非提醒。
- **繪製順序定案（產出 > 輸入）**：底圖 → 網格/起飛點 → 地面投影 →
  計畫路徑 →（deck）實測軌跡 → 機體圖示 → 標籤。**計畫是輸入、
  實測是產出，研究系統中產出不得被輸入遮蔽。**
| 4 | **路徑變粗糙** | 絲帶呈**灰白色扁帶、看不出分級色**——`billboard:true`（先前為解垂直段顆粒而改）使帶面永遠正對相機，**斜向段的帶寬被拉成一片**，顏色被亮邊吃掉 | **改回 `billboard:false`＋提高 `widthMinPixels`**（3→4）：貼地帶面才看得出分級色與路徑形狀；垂直段顆粒改用**同分級 run 的端點抽稀**處理，不靠 billboard |
| 5 | **標籤離機體太遠** | 標籤在圖示上方約 14px＋偏右 | **貼近至 6px、水平置中對齊圖示中心**（多機同位時仍依序錯開——§0.1 規則不變） |

**#2 與 #4 的關聯（重要）**：`billboard:true` 當初是為了解「垂直段顆粒」
而改，代價是斜向段變成正對相機的扁帶——**那正是使用者現在說的「粗糙」**。
兩者是同一個參數的兩面，**不能只改一邊**：改回 false 後必須同時處理
垂直段（端點抽稀），否則舊問題會回來。

**#4 再定案（2026-09-07，取代上列「改回 `billboard:false`」）**：使用者
反饋「**路徑線有些地方比較粗、有些地方比較細**」。粗細不一的三個來源
全部出在 `billboard:false`——寬度只在水平面展開，**投影到螢幕的壓縮量
取決於航向**（朝相機的段滿寬、橫過畫面的段被壓細）；圓形 joint/cap 躺在
水平面上，於是橫向段的線細、接點卻是滿徑的橢圓（截圖上一顆顆的瘤）；
再加上依幾何分流的兩層（斜/平段 false、垂直段 true）規則不同，交界必然
對不齊。**定案：全段 `billboard: true`、取消幾何分流（單一 PathLayer）。**

- **為什麼 2026-08-12 的「灰白扁帶」不會回來**：那次的帶寬上限是 8px，
  正對相機的寬帶彼此重疊才糊成一片；本次同時把 `widthMaxPixels` 收到
  **5**（下限維持 4），螢幕寬度 4–5px 的線不構成「面」，分級色照樣讀得出來
  （實測截圖：綠/黃/橙三級在同一條航跡上清楚可辨）。
- **代價（明記）**：帶面不再表達「這段在空中怎麼躺」。但該資訊本來就被
  俯角壓縮扭曲得讀不出來——**換到的是寬度處處相同的線**。垂直段可見
  （分流原本的目的）由 billboard 一併保住，端點抽稀維持不變。
- **#1 的 `billboard:false` 裁定不受影響**：那條講的是**機體圖示**，
  朝向語意優先；路徑線沒有朝向語意要保。
- **計畫絲帶收窄**：同輪反饋「**投影上去的預計路線太粗**」——`ribbon()`
  半寬 1.0→**0.45m**（厚度改為隨寬度走＝半寬的一半，否則窄帶配等厚的板
  會從側面看成立鰭）。**計畫是參照、實測是主角**，計畫帶比航跡線窄一截，
  兩者疊在一起才分得出誰是誰；收窄後底下的地面虛線也才露得出來。
  即時／回放／回放任務／比較四頁同步（同一個視覺元件不該有兩種粗細）。

### 2.4d 3D 機體模型（**使用者核准 2026-08-13，可實作**：真 3D＋縮遠降級）

**起因**：使用者反饋「**2D 圖示角度一偏就以為機不見了**」——貼地平面的
圖示在低俯角下投影面積趨近零。**這是本節要解的唯一問題**，任何方案
若在某個視角下仍會消失，即為不合格。

#### 方案（使用者已選 (b)：真 3D 模型＋縮遠降級）

| 距離 | 呈現 | 理由 |
|---|---|---|
| **近**（縮放 ≥ 門檻） | **真 3D 四旋翼 mesh**：機身＋四臂四旋翼，有實體高度 | 任何視角都有投影面積，解「消失」問題；立體感讓姿態可讀 |
| **遠**（縮放 < 門檻） | **降級回 2D 圖示**（現行 `droneIcon.ts`） | 遠距時 mesh 只佔數像素、細節無意義且耗資源；圖示有最小像素保證 |
| 切換 | 門檻附近**淡入淡出**（不硬切），避免縮放時的跳動 | |

#### 硬約束（不得違反）

1. **離線可用——模型不得外部載入**。專案的離線場域約束照舊。
   `droneIcon.ts` 已有前例（**程序化產生 data URI、無外部依賴**），
   3D 模型**同樣採程序化建構 mesh**（圓柱機身＋四臂＋四旋翼盤，
   `SimpleMeshLayer` 餵自建 geometry），**不引入 glTF 檔或 CDN**。
2. **識別色照舊**（§0.1 三槽＋灰）：模型主體材質著識別色；**文字標籤
   規則完全不變**（一律配機名、同位錯開）——3D 化不得取代標籤。
3. **heading 缺值不假旋轉**（§2.4b 既有規則）：無 heading 時模型
   **維持固定朝向並移除機頭指示**，不得用預設值假裝知道朝向。
   ——注意 §2.4c #2 使用者已裁「**不要機頭箭頭**」，故 3D 模型
   **本體不加箭頭**；朝向由機身幾何自然呈現（有 heading 才旋轉）。
4. **永不被覆蓋**（§2.4c #3 硬規則）：模型與標籤仍在最上層；
   **注意 3D mesh 有深度**，需確認它不被軌跡的 3D 幾何遮蔽
   （必要時關閉該層深度測試或提高繪製順序）。
5. **最小可見尺寸**：模型在任何俯角下的螢幕投影**不得小於 2D 圖示的
   最小尺寸**——這是本節的驗收判準，**不是美觀問題而是功能問題**。

#### 驗收方法（明訂，避免重蹈今日的觀察錯誤）

以**像素量測**驗證，不憑目視：於**俯角 0°／30°／55°／75°**（75＝
三頁 `maxPitch` 實測值，**驗收角度以使用者實際可達的視角為準**——
驗一個到不了的角度沒有意義；原稿寫 80° 是我憑印象取整、未查證）與
**縮放門檻上下各一級**共 8 組條件截圖，量測機體像素面積；
**任一組低於 2D 圖示基準即不合格**。多機時同時驗識別色可辨與標籤未重疊。

### 2.5 編隊（多機）模式——013 修訂，待使用者核准

**漸進顯示**：只有 ≥2 機連線時，面板對象行行尾才出現「⧉ 編隊」入口；
單機使用者永遠看不到本節任何 UI。

#### 文字圖——編隊設定

```
┌任務控制──────────────────────▾┐
│ 編隊 · 3 台          [↑ 全部起飛] │ ← 兩段式（變紅「確定？」）
│ ◎sim-uav-1  ◉swarm-2  ◉uav-3   │ ← 成員列：◎=焦點(看)、◉=目標集(指揮)
│ 任務  [ 同一路徑 ｜ 各自路徑 ]    │ ← segmented，不做前置選單
│ （同一）選擇任務▾   間距 5 m      │ ← v1：預設隊形＋間距輸入
│ （各自）● sim-uav-1  任務A ▾     │
│         ● swarm-2   任務B ▾     │
│ 高度 10 m                       │
│ 手動控制（編隊中停用）            │ ← 鎖定＋一句原因
└────────────────────────────────┘
```

- **看 vs 指揮分離**：點**地圖上的機體球**＝切焦點（◎，HUD/影像跟隨）；
  點**左上色點／成員列**＝toggle 目標集（◉，指令對象）。外圈=焦點、
  角標=目標集，兩者可不同機。
- **成員資格**：任何機可勾入編隊（規劃自由），唯一例外＝無指令通道機
  （mav_sysid=null，物理上發不了指令）不可勾。未驗證／低電／未就緒機
  可勾，但色點帶狀態環（warn 環／紅電池）預警。
- **統一路徑的誠實預覽**：地圖畫的是自動 offset 後**實際會飛的 N 條**
  識別色計畫線（3D 高度分層自然可見「不會相撞」）；使用者選的原始
  路徑退為灰虛線參考。絕不畫一條線假裝大家都飛它。高度差 hover 顯示，
  不加常駐標籤。

#### 文字圖——全部起飛進度

```
┌任務控制───────────────────────┐
│ 編隊起飛中…              [中止] │ ← 過程唯一互動：全撤
│ ● sim-uav-1   ✓ 已起飛          │
│ ● swarm-2     解鎖中…           │
│ ● uav-3       待命              │
└───────────────────────────────┘
失敗時：
│ ● swarm-2   ✗ 機端拒絕：Preflight GPS… │ ← 紅字原文（失敗必須看得見）
│ ● sim-uav-1  已撤回                    │
實機：
│ ● uav-3     等待現場確認               │ ← pending_approval，不倒數
```

- 全成 → 自動啟動任務；任一失敗 → 全撤（已解鎖者上鎖）＋逐行結果。
- **風險擋在行動點**：目標集含未驗證／低電／未就緒機時「全部起飛」
  disabled＋逐台原因行（「swarm-2：機型未驗證」「uav-3：電量 18%」，
  沿用 not_ready_reasons 樣式）；伺服器端同步嚴格 gate（UI 不是唯一防線）。

#### 狀態對照（後端契約 group-missions-design.md §7 → 畫面人話）

逐台 phase → 進度行文字：待命(idle)／上傳路徑中(uploading)／路徑已
上傳(uploaded)／解鎖中(arming)／已解鎖(armed)／啟動中(starting)／
✓ 飛行中(flying)／已降落(landed)；旁支：✗ 路徑上傳失敗／✗ 起飛檢查
未過／✗ 機端拒絕（紅字＋結構化原文 msg/hint/autopilot_notes）／返航中。

群組 status → 畫面：draft＝設定畫面；**pending_approval＝「等待現場
確認」列**（019 實機審批，不倒數）；executing＝進度清單（**非同步
202＋1s 輪詢驅動，無長凍結**）；flying＝全機 ✓ 後收合回正常監控；
**gate_rejected＝按下瞬間的同步失敗畫面**（面板保持設定態＋逐台
原因行，不進進度清單）；aborting＝「撤回中…」；aborted＝全撤結果
列表；**partial＝混合結果態**——續飛機保持監控、失敗機顯示原因，
不假裝全成或全敗。

「中止」鈕＝abort 端點：冪等單擊（緊急操作不二次確認），後端依
phase 自動選撤回序列；操作員中止與伺服器自動全撤終態一致，
畫面不區分來源、事件流記錄原因。

---

### 2.9 即時影像 PiP（022——使用者核准 2026-08-12，可實作）

- **觸發**：選中機有影像源時，右下浮出 16:9 小窗（比例尺上方，
  寬 ~280px），畫面跟隨選中機自動換源；無影像源＝不顯示（沒有就不畫）。
- **窗角常駐**：機隊識別色點＋機名（多機時「在看哪台」恆明確）。
  **此徽章不得因簡約原則移除**：測試流有大字色塊可辨機身，真相機階段
  三台畫面都是真實場景、可能極相似——徽章屆時是唯一的辨識依據
  （現場情報 2026-08-12）。
- **放大**：點小窗＝切全幅影像（現有「地圖↔影像」檢視收斂為此放大態，
  不另存兩套）；全幅點「地圖」返回。小窗可收合成 📹 鈕。
- **可自由拖曳**（使用者反饋 2026-08-12）：拖窗身移動（拖曳中不觸發
  放大——沿用縮圖卡的位移門檻：<5px 視為點擊）；**邊界夾限**不可拖出
  視窗（四邊各留 8px，上界避開導覽列）；**允許與 HUD／圖例／比例尺
  重疊，不做吸附避讓**——使用者主動拖到哪就是哪，系統不自作主張搬動；
  **位置記憶** localStorage（同任務控制面板慣例，key 獨立）；
  換機／收合／放大返回都回到記憶位置，不重置。放大態行為不變。
- **錄影狀態不加燈**：記錄燈 title 擴充「記錄中——遙測＋影像」；
  影像錄製異常＝warn 事件＋toast「影像錄製中斷——遙測與紀錄不受影響」。

## 3. 機隊頁（/drones）

### 3.1 文字圖

```
┌──────────────────────────────────────────────┐
│ ▸ ＋ 註冊無人機                               │ ← 摺疊（表單零解說文）
│ ● sim-uav-1   ▮▮▮▯ 🔋81%          27 次  ▸  │ ← 亮點=連線；訊號/電池即時
│ ○ swarm-2                          8 次  ▸  │ ← 灰點=離線；沒有的不畫
└──────────────────────────────────────────────┘
展開後（▾）：
│ ▾ sim-uav-1   ▮▮▮▯ 🔋81%          27 次     │
│   udpin://0.0.0.0:14540      [改名][影像][刪除] │
│   [PX4] [主機] [模擬]                          │ ← 資訊膠囊一行（不用・串接）
│   最近飛行 8/11 15:13                          │ ← 弱文字獨立一行
│   ┌ 架次表：時間/任務/時長/樣本/SINR/RTT/高度 ┐  │
│   └ 每列：[匯出][移除]                       ┘  │
```

### 3.2 配色

行底＝卡片色；連線點亮＝`#0ca30c`、離線＝`#8f8b80`；刪除／移除＝紅框；
飛行中 chip（唯一保留在收合行的狀態）＝綠底。

### 3.3 動作邏輯

| 操作 | 行為 |
|---|---|
| 點行 | 展開/收合（per 機記憶；單機自動展開） |
| 刪除 | modal 確認（顯示將刪 N 條航線；路徑不受影響）→ 執行 |
| 架次列點擊 | 進該架次回放頁 |
| 影像 | 設定 video_url（表單，零解說；「怎麼填」收 ⓘ） |

---

## 4. 路徑管理頁（/missions）——v2 已核准（使用者 2026-08-11：**採 B 案
等距投影 SVG**，所有卡可同時旋轉）

### 4.1 文字圖

```
┌──────────────────────────────────────────────┐
│ ┌────────┐ ┌────────┐ ┌────────┐ ┌───┐      │
│ │ 3D 縮圖 │ │ 3D 縮圖 │ │ 3D 縮圖 │ │ ＋ │      │
│ │(即時頁同│ │ 拖曳=旋轉│ │        │ │上傳│      │
│ │款視圖)  │ │ 不能平移 │ │        │ └───┘      │
│ │complex-│ │test-   │ │survey-2│            │
│ │survey  │ │flight  │ │        │            │
│ │  [顯示中]│ │        │ │    ⋯  │            │
│ └────────┘ └────────┘ └────────┘            │
└──────────────────────────────────────────────┘
```

### 4.2 顯示內容與配色

| 元素 | 內容 |
|---|---|
| **3D 縮圖** | 即時頁同款 3D 視圖（暖畫布＋網格＋灰絲帶航線＋起點綠/終點方塊、傾斜 55° 起始）；**只能旋轉且 360° 無限制**（卡上拖曳＝繞路徑中心自由旋轉，bearing 不設夾角——使用者指示 2026-08-11），**不能平移/縮放**，鏡頭恆自動 fit 整條路徑；滾輪穿透給頁面捲動 |
| 名稱 | 卡底一行，主文字色 |
| 顯示中標記 | 當前疊到即時頁的卡片亮珊瑚框＋「顯示中」chip |
| ⋯ 選單 | 顯示於即時頁／隱藏、刪除（兩段式）|
| ＋ 上傳卡 | 虛線框卡片，點擊選 .plan 檔 |

**這一頁不放的兩樣東西**（2026-09-07 使用者指示）：

* **「從機上讀回」按鈕**——`POST /api/missions/from-vehicle` 還在，
  rig 與 curl 叫得到，拿掉的是畫面上的入口。
* **幾何預檢報告**——`GET /api/missions/{id}/check` 與上傳回應的 `check`
  都還在，而**真正的守門也還在**：指令服務上傳到機上之前會自己檢查，
  沒過就是 `rejected_precheck`，理由出現在任務控制面板。這一頁拿掉的是
  「還沒要飛之前先讀一遍報告」那一層，不是安全網本身。

**效能約束（實作提示）**：多卡不可各開 WebGL context（瀏覽器上限
8–16 個）——共用單一離屏 renderer：預設顯示**預渲染靜態圖**（開頁時
逐卡渲一張 3D 視角快照），只有**拖曳中的那張卡**掛 live 渲染、
放開即回快照。同時 live 的卡最多 1 張。

**實作兩案（前端預評 2026-08-11，待使用者選）**：
- **A 案·真同源渲染器**（估 1.5–2 工作段）：隱藏 maplibre 實例產快照
  ＋拖曳時 portal 覆蓋 live（bearing 錨路徑中心、scrollZoom 停用、
  確定性 fit）——與即時頁像素同源，一次只能一張卡 live。
- **B 案·等距投影 SVG**（估 0.5 工作段）：waypoints 固定視角等距投影
  畫 SVG（暖畫布＋網格＋絲帶都近似呈現），拖曳＝重算重繪——「看起來
  同款」的近似、非同一渲染器，但零 context 限制、**所有卡可同時
  旋轉**、成本 1/3。
  取捨：要「就是同一個 3D」選 A；要「同款觀感＋更順的多卡互動」選 B。

### 4.3b 「改成地形跟隨」（2026-09-07，issues/047 §1-A）

卡片 `⋯` 選單裡的一項。把每個航點的高度從「離起飛點」改寫成「離地面」
（`frame 10`），由飛控用自己的地形圖庫跟著地面飛。

**存成新的一份，不就地改寫。** 改寫之後高度的意思變了——那是另一份航線，
操作員應該先看到它的縮圖、跟原本那份比對過，再決定要不要飛。原本那份不動。

**做不到的時候要說出是哪一個航點。** 查不到地形高程、或某個航點本來就在
地面下，一律整份不轉（一半一個基準的航線比不轉更危險），錯誤訊息指名 seq。

**上傳時才會知道能不能飛**：地形跟隨要飛控的設定撐得住，而那要問到機才算數
——尤其是返航高度（地形資料一斷，飛控就照「離起飛點 N m」飛回來）。
被擋時訊息說得出至少要多少。

### 4.4 每條航線的飛行約束（使用者 2026-08-13：從 .env 移到 plan 頁、
**逐 plan 動態設定**；待核准）

**資料模型變更（不只是搬位置）**：現況是全域三個常數
（`geofence_radius_m` 50／`geofence_alt_m` 15／`geofence_margin` 0.7），
**所有航線共用**。使用者要的是**每個 .plan 各自的約束**——這是
**一對多**：約束隨航線走、不同場域不同值。後端需 `missions` 加欄位＋
API，**全域值降為「新航線的預設起始值」**。

#### 使用者流程（重新設計）

```
路徑頁卡片 → ⋯ →「飛行約束」        ← 入口在卡片選單，不另設頁面
┌ 飛行約束 — complex-survey ──── ✕ ┐
│ 半徑上限   [  50 ] m              │ ← 預填目前生效值
│ 高度上限   [  15 ] m              │
│ 餘裕比例   [ 0.7 ]                │
│ ─────────────────────────────── │
│ ⚠ 此航線目前有 3 個航點超出新半徑  │ ← 即時預檢（改值當下就算）
│   （最遠 62m）                    │
│ 上次變更 8/13 14:02 · 50→40m ⊕全域 │ ← 含全域傳播（見下），⊕標來源
│              [取消]  [儲存並重檢] │ ← 危險操作：兩段式
└──────────────────────────────────┘
```

#### 規則

1. **入口在路徑頁卡片的 ⋯ 選單**（「飛行約束」）——**不新開頁面**：
   約束屬於航線，就住在航線旁邊（單一住所）。
2. **改值當下即時預檢**（不必等儲存）：顯示**這條航線有幾個航點會超出
   新值、最遠多少**——**讓後果在決定前可見，而不是儲存後才發現**。
3. **儲存採兩段式確認**（危險操作慣例）：按鈕文字「儲存並重檢」，
   點擊後變紅「確定？」。**理由：這是安全參數**，改鬆等於放寬飛行邊界。
4. **顯示目前生效值與最近一次修改**（時間＋新舊值）——**改過什麼要
   看得見**；完整歷程若後端有存，放 ⋯ 內不佔版面。
5. **既有航線的預檢結果會變**（使用者關切點）：儲存後**該航線的
   顯示中/預檢狀態立即重算**，並在卡片上標示「**約束已變更，預檢已重算**」
   直到使用者下次開啟該航線（一次性提示，不常駐）。
6. **未設定的航線沿用全域預設**，且**明示是預設值而非該航線自訂**
   （欄位旁弱字「使用預設」）——**分辨「沒設過」與「設成剛好等於預設」**
   是誠實原則的直接應用。

#### 資料契約（三方討論定案 2026-08-13，PM 主持）

- **Q1 既有航線遷移＝全部 null**（不填入當前全域值）——填值會把
  「從未設定」偽裝成「設過且剛好等於預設」，正是本設計要分辨的。
- **Q3 null 的語意＝跟隨現行預設（動態），非凍結建立時的值**——
  「沿用」的本義。**因此改動全域預設會傳播到所有 null 航線**，
  **UI 必須警示（見下）**。
- **Q2 修改紀錄＝append-only 留痕表**（後端定案）：
  `geofence_changes(time, scope['global'|'mission'], mission_id, client,
  old_value, new_value, **affected_mission_ids**)`。兩項補強：
  **全域預設的改動也留痕**（scope=global，影響面最大者不可不記）；
  **改動當下快照影響範圍**（事後重算得到的是今天的 N、不是當時的 N）。

#### 「上次變更」的顯示範圍（UI 裁量，2026-08-13）

**涵蓋兩種來源，且必須標明是哪一種**：

| 來源 | 顯示 |
|---|---|
| 本航線設定 | `8/13 14:02 · 半徑 50→40m` |
| 全域預設傳播（當時 affected 含本航線） | `8/13 14:02 · 半徑 50→40m ⊕全域` |

**為什麼要涵蓋全域**（採 PM 直覺）：對操作者而言「**我的航線邊界變了**」
不分來源；只顯示 mission-scope 會**漏掉最大宗的變因**——使用者會看到
預檢結果變了卻找不到任何修改紀錄，那比不顯示更糟。

**為什麼必須標來源**：兩者的**可回復性不同**——本航線的改動改回來只
影響自己；全域傳播來的，改回本航線值等於**脫離「沿用」變成明示設定**，
語意變了。**不標來源，使用者會用錯誤的心智模型去修正它。**

**排序**：兩種來源混合後**按時間取最近一次**（不分來源優先）——
因為使用者要問的是「上一次變成現在這樣是什麼時候」。完整歷程（含
兩種來源、可展開 affected 清單）放 ⋯ 選單，不佔卡片版面。
- 欄位：`missions.fence_radius_m` / `fence_alt_m` / `fence_margin`
  （nullable）；`PATCH /api/missions/{id}/fence`；預檢 API 改讀該航線值。
- schema 變更**同批更新 `doc/data-schema.md`**（既有慣例）。

#### 全域預設變更的傳播警示（Q3 定案的 UI 義務）

全域預設將從 `.env` 搬進系統設定（與本案是同一件事的兩層）。
**修改全域預設時必須警示傳播範圍**：

```
┌ 系統設定 · 飛行約束預設 ──────── ✕ ┐
│ 半徑上限  [ 50 ] → [ 40 ] m        │
│ ⚠ 此變更將影響 7 條使用預設的航線   │ ← 數量必顯
│   ▸ 查看清單                       │ ← 展開列出哪 7 條（可點進該航線）
│   其中 2 條的航點將超出新半徑 ▸     │ ← 後果預檢，非只列名單
│            [取消]  [變更並重檢]    │ ← 兩段式（安全參數）
└───────────────────────────────────┘
```

**設計理由**：null 的傳播是刻意的（「沿用」就該跟隨），但**傳播必須是
知情的**——使用者改一個數字可能同時放寬多條航線的飛行邊界。
**只顯示數量不夠，要能看見是哪幾條、以及哪幾條會因此超界**——
**後果在決定前可見**，與 §4.4 逐航線改值時的即時預檢同一原則。

### 4.5 任務折線：四個畫面同一個形狀（2026-09-07 使用者回報）

任務航線出現在四個地方：即時頁疊圖、兩個回放頁、路徑管理頁的 3D 縮圖。
**折線的補點邏輯只有一份**（`lib/geo.ts` 的 `planPath`）——縮圖曾經自己寫過
第二份，於是同一份 .plan 在縮圖上是另一個形狀。

兩個要補的段落，因為 .plan 裡它們沒有座標或沒有地面點：

* **起飛段**：`NAV_TAKEOFF` 的高度是「爬到哪」，不是「它所在的高度」。
  直接畫，折線會從空中出發。座標是 0,0 時意思是「從 home 起飛」。
* **收尾段——三種寫法意思完全不同，不得混為一談**：

  | 項目 | 意思 | 怎麼畫 |
  |---|---|---|
  | `RTL`（cmd 20） | 回 home 再降 | 平飛回 home → 垂直下降 |
  | `LAND` 有座標 | 飛到那個點再降 | 平飛到該點 → 垂直下降 |
  | `LAND` 沒座標 | **在當下的位置降**（不是回 home）| 在最後一點垂直下降 |

  原本一律當成「回 home 降落」，於是一份**降落點與起飛點不同**的航線，
  畫出來會從降落點再拉一條線回到起飛點——**那條線不在任何一份 .plan 裡**。

補點一律**兩段式**（先平飛、再垂直升降），不畫斜線：斜線會讓人以為航線
會穿過中間的地形。航點沒帶高度時沿用上一個，**不掉回 0**——那會憑空插一段
俯衝到地面再爬回來。

縮圖端配套：高度缺值畫在 **0（地面）不是 10 m**。那個 10 是元件自己的預設值，
一份全程 1 m 的低空航線會因此看起來忽高忽低。

### 4.3 動作邏輯（v3 修訂，使用者指示 2026-08-11）

| 操作 | 行為 |
|---|---|
| **左鍵點卡片** | 卡片下方展開**使用紀錄**：被使用次數＋哪幾台無人機使用過（機名列點）＋架次列表（點列進回放）。再點收合。**展開中的卡片加選中外框**（`--ink` 暖白 1.5px，與「顯示中」珊瑚框視覺區分；使用者定案 2026-08-11：無外框會讓人懷疑點錯卡）。不再是「顯示於即時頁」切換 |
| **顯示於即時頁** | **自動**：任務開始（起飛→任務／啟動任務／編隊執行）時該任務自動浮現於即時頁疊圖，毋須手動設定；手動切換降級收入 ⋯ 選單（保留能力） |
| 刪除 | 兩段式（卡上變紅「確定刪除？」），在 ⋯ 選單 |
| 上傳 | 檔案選擇→上傳→新卡出現；失敗原因原文顯示（失敗必須看得見） |

使用紀錄資料源：sessions 按 mission_id 聚合（次數＋distinct 機名），
現有 /api/sessions 可算；020 修好後自動涵蓋新飛行。

---

## 5. 回放頁（/replay/[id]）——抽屜化未實作，核准後動工

### 5.1 文字圖

```
┌──────────────────────────────────────────────┐
│ ←  8/11 14:42 · test-flight                  │ ← 返回＋日期＋任務名（無樣本數）
│                                              │
│              （全幅 3D 地圖）                  │
│         航跡絲帶（訊號分級色）＋方向箭頭          │
│         球體游標（隨時間軸移動）                 │
│                                    20 m ▭    │
│ ▶ ──────●───────────────  ▲38m ▮▮▯▯        │ ← 播放/暫停＋時間軸＋游標處高度/訊號格
│ 〓 圖表                                    ▲ │ ← 上滑抽屜（展開記憶）
└──────────────────────────────────────────────┘
抽屜展開：SINR 時序圖＋RTT 時序圖（共用時間軸、事件三角標記 WARNING↑）
```

### 5.1b 方向箭頭（2026-09-07 使用者裁定）

**箭頭貼在航跡線正上方、尺寸隨縮放走。** 原本是世界座標的 6m 三角形浮在
絲帶上方 0.7–1.2m：公尺尺寸在近距縮放下變成一片大白板（「太大」），
6m 的長度又讓它戳出線外、看起來是獨立物件而不是線的一部分。

- **落點**：與航跡線同一個 3D 座標，往螢幕上方推 6px——**貼著線的上緣**。
  壓在線上會被分級色包住、小尺寸下認不出是箭頭。
- **尺寸**：`IconLayer` 公尺錨定（1.2m）＋像素夾限 **7–22px**。物理尺寸
  刻意取小，讓常用縮放落在夾限**之間**——公尺數太大就整段貼在上限，
  等於固定螢幕尺寸、縮放沒有反應。
- **朝向**：`billboard:false` ＋ `getAngle`＝航向（羅盤順時針取負；deck 是
  逆時針）。箭頭有方向語意，帶面必須貼地才會指向真正的地面方位——
  同 §2.4c #1 對機體圖示的裁定。傾斜視角的壓縮用同一套 cos^-0.75 補償，
  故**視角轉動（pitchend）後要重推圖層**，否則停在舊補償係數。
- **航向來源**：往後找到第一個水平位移 ≥1m 的樣本；1Hz 資料在懸停時
  相鄰兩點的差幾乎全是 GPS 抖動。找不到就**不放這一枚**——方向不明時不指。

### 5.2 配色

時間軸軌 `#3a3833`、已播進度 `#d97757`（互動元件）、游標球白框；
圖表線 `#3987e5`（單系列）、事件三角＝severity 色；其餘同全域。

### 5.3 動作邏輯

| 操作 | 行為 |
|---|---|
| ▶／空白鍵 | 播放/暫停（1×，速度選項收 ⋯） |
| 拖時間軸／點圖表 | 跳至該時刻，地圖球體游標同步 |
| 時間軸游標移動 | 右側 ▲高度＋訊號格即時跟隨（游標處數值） |
| 抽屜 | 上滑展開／下滑收合，狀態記憶 |
| 點事件三角 | 跳至事件時刻＋tooltip 顯示人話句 |

---

### 5.4 回放影片同步（022 重點——使用者核准 2026-08-12，可實作）

```
┌ 回放頁 ─────────────────────────────┐
│（全幅地圖＋軌跡）      ┌影片 PiP────┐ │
│                      │ ●sim-uav-1 │ │ ← 窗角識別色點＋機名
│                      │  [影像]    │ │
│                      │▁▂▁色帶▄▅▂▁│ │ ← 底緣 3px SINR 同步色帶
│                      └───────────┘ │
│ ▶ ──█████──░░──█████────── ▲38m   │ ← 時間軸＋影像涵蓋帶
│      ↑有影像   ↑缺口  ↑有影像        │
└─────────────────────────────────────┘
```

- **雙向同步**：拖時間軸／點圖表 → 影片 seek 至對應段內時刻（依每段
  絕對時間錨點）；影片播放 → 時間軸游標＋地圖球體＋圖表 crosshair
  跟走；播放速度選項同步影片倍速。
- **時鐘源硬約束（前端 2026-08-12 提出的風險，列入驗收）**：段錨點
  `started_at` 必須與遙測時戳**同一時鐘源**（皆為 backend/DB 的 UTC）。
  若錨點取自錄製容器本地鐘，兩者的時鐘偏移會讓整段影片與資料**恆定
  偏移**——畫面看起來會播、但對照到的 SINR 是別的時刻的，**這種錯誤
  比明顯故障更危險**（研究結論會被靜默污染）。驗收必測：以可辨識瞬間
  （起飛離地／訊號劣化段畫面變糊）比對影片時刻與遙測事件時刻，偏移
  須 < 1s；若系統性偏移，屬阻斷級。**模擬環境實測已過**（後端
  2026-08-12：三容器共用核心時鐘，段結尾 vs 收錄指令 −0.192s≈0；
  段起始的 +1.371s 是等關鍵影格、不造成錨點錯誤）。

- **⚠️ 管線延遲：與研究變數相關的偏移（真機階段必測，不可只測一次）**
  `started_at` 的語意是「**影格抵達地面站的時刻**」，不是「**相機拍下
  的時刻**」，兩者差一個管線延遲（編碼＋傳輸＋抖動緩衝）。
  **模擬環境基線實測 0.41s（方向恆定：定位到 T 會看到 T+0.41s）**
  ——非時鐘錯配、亦非等關鍵影格（後端 GOP 1s vs 4s 對照實驗：
  +1.6s vs +1.55s，與 GOP 無關，故「等第一個 I-frame」的假設被推翻），
  屬錄製器「開始錄」與「第一幀落地」之間的固定管線延遲。
  **現階段不做補償**：硬編 0.41s 在真相機上必錯（編碼器/GOP/網路皆異），
  正解（讀第一幀時戳蓋錨）需 backend 具備媒體探測能力、屬架構改動。
  **改為明確記載基線**，真機階段量增量；**真 5G 上此延遲會隨鏈路劣化而變大——而鏈路劣化正是
  本專案的研究自變數**。因此它不是可事後校正的固定偏移，而是**與被
  研究變數相關的偏移**：畫面愈糊的時刻時戳愈晚，「畫質劣化 vs SINR」
  的對應會被系統性往後拖；且它在好鏈路時完全正常，最難察覺。
  - **UI 誠實義務**：影片窗與回放頁的說明處標示「影片時間＝抵達地面站
    時刻，與拍攝時刻差一個傳輸延遲」；**不假裝精準**。
  - **真機驗收方法（升級版）**：以畫面可辨識瞬間比對遙測事件，
    **分別在好鏈路與劣化鏈路各測一次**——若兩者偏移不同，即證實延遲
    與鏈路相關，須在 UI 標示不確定範圍，並評估 Phase 3 機端時碼
    （RTCP sender report NTP 對映或機上燒時碼）。
- **尾段 3 秒是刻意保留、不得裁切**（後端 `on_session_end` 在 disarmed
  後 sleep 3s 才收錄，避免切掉落地那一刻——那通常最該看）。實測影片
  比遙測晚收 +3.7~3.8s（3s 常數＋PATCH 往返＋flush）。**UI 處置**：
  涵蓋帶照實畫到影片實際結束（不對齊遙測結尾、不裁切）；影片時間軸
  超出遙測範圍的尾段，資料側游標停在最後一筆遙測即可（**不外插假資料**）。
  若後端輸出 `tail_pad_s`，回放頁說明處可標「落地後多錄 N 秒」——
  但**預設不特別解釋**（多錄不是異常，簡約優先）。
- **`final` 旗標：未結算的尾端不得斷言為缺口**（後端 2026-08-12 新增）。
  片段長度在落地後才收斂（暫時值→最終值），`final=false` 期間該段尾端
  的「還沒算完」與「真的沒錄到」在資料上**無法區分**。UI 規則：
  - `final=false` 的段，**尾端不畫涵蓋帶缺口、不顯「此時段無影像
    （錄製中斷）」**；scrub 進該區間顯示中性句「**影像處理中**」
    （非空態句、非故障句——它既不是沒錄到也不是壞掉，就是還沒算完）。
  - **涵蓋帶的第三種視覺**（前端補位、核可）：未定案尾端**留白＝讀作
    缺口（斷言沒錄到）、實心＝謊稱有影像**，兩者皆錯——故用
    **半透明條紋延伸至軸末**，與實心帶明顯可辨，語意＝「這段還不知道」。
    三態視覺與三種語意一一對應，不共用。
  - **必要配套：`final=false` 時 5 秒輪詢直到定案**（前端補位、核可）。
    無輪詢則「定案後重畫」永不發生——使用者不重整就永遠停在未定案
    視覺，規則等於失效。**規則本身若依賴某個狀態變化，就必須指定
    它如何被觀察到**，此為規格漏寫，已補。
  - 收斂為 `final=true` 後，該段依實際長度重畫涵蓋帶；此時若仍有缺口
    才是真缺口，才可顯「錄製中斷」。
  - **理由**：把「還在結算」說成「錄製中斷」＝把正常說成故障，與
    `missing`/`off` 不得混用同一原則。
  - **設計取捨紀錄**：UI/UX 原建議「未定案前不回該段」被後端否決且
    否決正確——不回傳會讓 `video_status` 算成 `missing`（整趟被說成
    錄製失敗），從「少給」變成「說謊」，比原問題更糟。**`final` 旗標
    是把判斷所需的事實給 UI，而不是要 UI 猜**，這是正解。
- **影像涵蓋帶**：時間軸上 3px 薄帶標示有影像的時段——fmp4 分段
  ＝帶分段，**段間空白就是真空白**（與後端「不得靜默拼接」硬約束成對）。
- **五種錄影狀態**：API 欄位為 **`video_status`**（後端算好；DB 內部欄位
  `video_mode` 只有 on/off/no_source 三值、**不外露**——它不知道「過期」
  與「該錄卻沒錄到」，那兩態需架次時間×保留天數推導，故一律後端算，
  **UI 不做日期運算**）。回放 API 形狀：
  `{retention_days, video_status, segments[{id, started_at, duration_s, url, …}]}`。
  五態**嚴格分句、不得混用**：

  | 狀態 | UI 呈現 |
  |---|---|
  | `available` | 播放器＋涵蓋帶。**有缺口不是空態**：帶上照實留白，scrub 進缺口才顯「此時段無影像（錄製中斷）」——「錄了但斷流」與「沒錄」意義相反，永不同句 |
  | `off`（刻意未錄） | 影片窗不顯示；header 弱字「本趟未錄影」；架次表同標 |
  | `no_source`（這台無影像來源） | 影片窗不顯示、不提示（沒有就不畫） |
  | `expired`（過保留期已清除） | 「影像已過保留期（{retention_days} 天），已清除」＋不顯涵蓋帶。天數動態帶入——後端已把它接上單一事實源（`.env` 一個變數同時餵清檔器與 API），UI 顯示不可能與實際清檔行為不一致 |
  | `missing`（**保留期內該錄卻整趟無流＝故障**） | **不用空態句**——空態句會把故障說成正常。走告警樣式：影片窗位置顯 warn 底卡「應有影像但未錄到（錄製故障）」＋事件流留 warn 事件，與 §2.9 的錄製異常 toast 同語言 |

- **畫質＝證據原件**：影片不轉碼原樣播放（後端定案互引）；SINR 色帶
  是**播放器 UI 疊層，絕不燒進影片檔**。畫面劣化與色帶對照＝研究證據。
- **多機 v1**：影片跟架次走（回放頁本為單架次頁），單窗；群飛多窗
  並排留待真實需求（明示界線，不預建）。
- 機隊頁架次表：opt-out 架次加弱字「未錄影」標記（有錄的不標——
  正常不宣告）。

## 6. 場域訊號頁（/compare 重定義）——v4「開頁即答案」
（使用者核准 2026-08-11：零步驟／A+B 疊加／砍表砍圖——可實作）

### 6.1 定位

不再是「比較工具」，是**場域訊號地圖**：開頁直接回答「這個場域哪裡
訊號弱」。視覺與操作語言＝即時頁同款（全幅 3D 地圖＋浮動面板），
不再是圖表分析頁。架次表、沿線圖、斷訊率欄全部移除。

### 6.2 文字圖

```
┌──────────────────────────────────────────────┐
│ 導覽列                                        │
│                          ┌場域訊號──────▾┐   │ ← 浮動面板（任務控制同款）
│   （全幅 3D 地圖，即時頁     │ 時間 [7天|30天|全部]│
│    同款畫布/網格/起飛點）    │ 航線 [全部 ▾]      │ ← 篩選全部可選，非前置
│                          │ 機   [全部 ▾]      │
│      ▓▓▓░░  ← A 軌跡累積   │ ☑ 弱區輪廓         │
│     ╭─────╮              └───────────────┘   │
│     │▼-15 │ ← B 弱區輪廓＋最差值               │
│     ╰─────╯                                  │
│ ┌圖例────────┐                               │
│ │四分級＋      │                               │
│ │「淡=單趟,   │                     50 m ▭   │
│ │ 濃=多趟一致」│                               │
│ └───────────┘                               │
└──────────────────────────────────────────────┘
```

### 6.3 顯示內容

| 元素 | 內容 |
|---|---|
| **A 軌跡累積** | 開頁即載入篩選範圍內（預設最近 30 天）全部架次軌跡，deck.gl 絲帶、分級色、**單趟低透明（~25%）**——多趟一致的弱區紅色越疊越濃，濃淡天然表達「每次都爛 vs 偶爾爛」（環境性 vs 時變性不再需要口訣） |
| **B 弱區輪廓**（開關，預設開） | 由格網聚合（10m、P10）圈出**持續性弱區**：P10 入劣化以下且涵蓋 ≥2 趟的相鄰格聚簇 → 平滑輪廓線＋圈上標最差值（▼-15）；**只圈飛過的區域**（誠實：聚合底層同 query_signal_map 契約） |
| 浮動面板 | 任務控制同款（可收合/拖曳）：時間範圍 chips（7天/30天/全部）＋航線下拉（全部/單選）＋機下拉＋輪廓開關。全部可選、無必選 |
| 圖例 | 四分級＋一行「淡＝單趟．濃＝多趟一致」＋輪廓樣式說明 |
| 地圖 | 即時頁同款：暖畫布、50m 網格、起飛點錨定、傾斜 3D、Ctrl 縮放、拖曳旋轉 |

### 6.4 動作邏輯

| 操作 | 行為 |
|---|---|
| 開頁 | **零步驟**：最近 30 天全場域累積圖＋輪廓直接呈現 |
| 篩選 | 換時間/航線/機 → 累積圖與輪廓即時重算 |
| **點弱區輪廓** | 資訊卡：涵蓋趟數／樣本數／最差值／最近一次劣化時間 |
| **點軌跡** | 該趟資訊卡：時間＋機＋**備註 ✎（inline 編輯，PATCH sessions.note）**＋「開回放」鈕——備註功能的家從架次表移到這裡 |
| 點空白 | 關資訊卡 |

### 6.5 資料契約（調整）

- **query_signal_map 共用查詢層照舊**（輪廓的聚合底層；前端已有客端
  同形實作，後端落地換源）。
- **sessions.note PATCH 照舊**（軌跡點擊卡使用）。
- **斷訊率摘要欄取消**（v3 架次表已砍，無呈現處；後端排程項目移除）。
- 軌跡載入：GET /api/sessions?since=＋各 session track——多趟載入量大，
  前端可分批/抽稀（繪製抽稀不影響聚合口徑）。

## 6b. 前後比較頁（/compare/ab）——023 相同路徑前後任務訊號比較
（**使用者核准 2026-08-12，可實作**；CDF 經使用者二次否決、已移除）

### 6b.0 與既有頁的關係（盤點）

**⚠️ 入口缺失（使用者 2026-08-13 反饋：「比較頁面沒辦法看熱圖與前後對比」）**：
導覽列第四個 icon 指向 `/compare`（場域訊號頁），而 `/compare/ab`
**沒有任何 UI 入口**——只能直接輸入網址。**使用者心目中的「比較頁」
就是導覽列那顆 icon**，所以他點進去看到累積軌跡、找不到熱圖與前後對比，
結論是「這頁沒有那些功能」。**這是我的資訊架構失誤**：我把兩個不同
用途的頁面放在同一個入口位置，卻只給其中一個入口。

**定案（待核准）**：場域訊號頁**頂部加一列檢視切換**
`[ 場域訊號 ｜ 前後比較 ]`——兩者共用導覽列同一顆 icon（它們同屬
「回頭看資料」的情境），切換即換頁；前後比較頁同樣有此列可切回。
**理由**：不新增導覽 icon（§1 四 icon 已定），也不靠使用者記網址；
**同一入口下的兩個檢視，用檢視切換而非隱藏連結**。

**入口設計判準（前端 2026-08-13 提出，採為通則）**：
> **使用者為了到這個功能，需要先知道什麼？需要先知道的越多，
> 那個入口就越接近不存在。**

本案失誤即為實例：原設計「從軌跡卡進入」需要三層前置知識
（軌跡可點→點了有卡→卡裡有連結），**規格寫了、程式做了、雙方都可
宣稱完成，但使用者到不了**——這比「忘記做入口」更難發現。

**新開一頁，不改場域訊號頁（§6 v4）。** 兩者回答的問題不同：
v4＝「這個場域哪裡弱」（多趟累積、無基準）；本頁＝「**這條路徑，
改善措施前後差多少**」（兩趟對照、有前後之分）。使用者曾明確否決
「比較頁塞圖表」，故本頁**不是 v4 的回退**，而是有明確研究任務的
分析頁；入口從 v4 的軌跡卡或路徑頁使用紀錄「比對回放」進入。

| 可沿用 | 需新做 |
|---|---|
| `lib/signalMap.ts`：`aggregateCells`（網格聚合）、`dropoutPct` | 弧長投影（樣本→路徑里程） |
| `FieldMap.tsx` 地圖底＋暖畫布＋網格＋起飛點 | 差值熱區（發散色盤疊圖） |
| 架次選擇（v3 膠囊列）、`sessions.note` 備註 | CDF 計算與 p5/p50 標記 |
| 識別色前二槽（藍/橘，已過 CVD 驗證） | 摘要表（含樣本數） |

### 6b.1 版面

```
┌─────────────────────────────────────────────────┐
│ 路徑：[3D縮圖列]   前：(●8/11 14:27 改善前) ✎    │
│                    後：(●8/12 09:03 調整天線後) ✎│ ← 各一顆膠囊，備註即標籤
├─────────────────────────────────────────────────┤
│ ① 沿路徑訊號（主圖）                              │
│   dB                                            │
│   15 ┤   ╭─前(藍)                                │
│    5 ┤ ╱ ╰──╮ ╭─後(橘)      ▒差值帶（後−前）      │
│   -5 ┤      ╰─╯                                 │
│      └────────────────────── 路徑里程 0–307m     │
│   ⓘ RSRP 對照：▁▁▁▁ 持平 → 外部雜訊升高特徵（判讀）│
├─────────────────────────────────────────────────┤
│ ② 差值熱區（俯視場域，全寬）                      │
│   ▨▨░░░  惡化 ← 灰中點 → 改善（發散色盤）         │
│   ▨▓▓░░  兩趟都有樣本才上色；單趟＝斜線「無對照」  │
│   ░▓▨▨▨  兩趟皆無＝不畫                          │
├─────────────────────────────────────────────────┤
│ 摘要表（唯一的分佈資訊呈現）                       │
│        均值    p50    **p5**    樣本數           │
│  前    8.2    9.1    -3.4     1,204            │
│  後   10.5   11.0     2.1     1,187            │
│  Δ    +2.3   +1.9   **+5.5**    —              │
└─────────────────────────────────────────────────┘
```

### 6b.2 三張圖的規則

**① 沿路徑訊號（主圖）**
- X 軸＝**路徑弧長（里程 m）**，非時間——兩趟速度不同，時間對齊會錯位。
- 兩條線＝識別色前二槽（前 `#3987e5` 藍／後 `#d95926` 橘，CVD 實測
  ΔE 26.8 protan／31.8 normal，通過）；**差值帶**＝兩線之間填色，
  後優於前填藍向、劣於前填橘向（同兩色，不引入第三色）。
- 原始淡線＋平滑線並存（既有誠實規則），窗長標示於卡頭。
- **單一 y 軸**（dataviz 硬規則：絕不雙軸）。RSRP 對照見下。

**② 摘要表（取代 CDF——使用者 2026-08-12 二次否決該圖）**
- 分佈資訊**以數字呈現、不畫圖**：均值／p50／**p5**／Δ／樣本數。
- **p5 仍是重點欄位**（加粗）：尾部惡化比平均更有研究意義——平均掉
  2dB 無感、最差時刻掉 10dB 就斷鏈。**資訊保留，只是不用一張圖來載。**
- 決策紀錄：CDF 曾於 §6 v3→v4 被否決，本案再次提出並附「情境不同＋
  p5 用途」論證，使用者仍選「主圖＋熱區就夠」。**明著問而非偷渡，
  結果否決但決策乾淨**——同類情形照此辦理。

**③ 差值熱區（全寬）**
- 網格 ΔSINR（後−前），**發散色盤：兩極＋灰中點**（dataviz 硬規則，
  中點絕不用第三個色相）；沿用場域頁的 `aggregateCells`，值改為差值。
- **兩趟都有樣本的格才上色**；只有一趟有樣本＝斜線紋理標「無對照」；
  兩趟都無＝不畫（誠實原則，與 v4 一致）。
- 格 hover：前/後各自的值與樣本數、Δ。

### 6b.3 RSRP 對照的呈現（方法論入 UI）

**規則：RSRP 持平＋SINR 降＝符合外部雜訊升高的特徵；RSRP 也降＝
變因在訊號強度側（距離／遮蔽／發射端）。**

**⚠️ 語意中性化（使用者 2026-08-13 定案）**：全站移除「干擾區」等
**先驗標注**——**機只知道訊號變差，不知道原因**；系統若標定「這裡是
干擾區」，等於把推測當事實、會導致誤判。**資料驅動的呈現不動**
（弱訊號區、分級色、熱區圖都是量測結果）；動的是**斷言原因的字眼**。
判讀句因此改用**現象描述而非成因結論**：「符合外部雜訊升高的特徵」
——它陳述的是可觀測的物理關係（RSRP 持平而 SINR 降），**把「是不是
干擾」留給研究者判斷**。
不要求使用者記這條規則，UI 直接呈現：

- **不做雙軸**（絕對禁止）。改為主圖下方一條**極簡 RSRP 迷你帶**
  （sparkline 高度 ~24px，同 X 軸對齊，僅畫前後兩趟的差值走勢）。
- **一句自動判讀**放在主圖卡頭 `ⓘ`：依整段中位數判定——
  「RSRP 大致持平而 SINR 下降 → **符合外部雜訊升高的特徵**」／
  「RSRP 同步下降 → **變因在訊號強度側**（距離、遮蔽或發射端）」／「兩者皆持平 →
  **無顯著變化**」。
- **判讀句必須可反駁**：句尾附依據數值（ΔRSRP 中位數 x.x dB、
  ΔSINR 中位數 y.y dB），使用者能自行檢查系統的結論。**系統給判讀，
  但不隱藏依據**——這是自動判讀不變成黑箱的唯一方式。
- **局部變化分支（2026-08-12 補；原規格缺陷）**：僅以「整段中位數」
  判定會在**變化集中於局部區段**時說出「無顯著變化」，而同頁摘要表
  的 Δp5 卻顯示明顯改善——**兩者各自正確卻互相打臉，使用者不知信誰**。
  規則：**逐段差值中位數持平，但 |Δp5| ≥ 3 dB **或** |ΔP50| ≥ 3 dB 時**，
  改判「整段差值持平，但（最差 5%／整體中位數）改善/惡化 X.X dB →
  變化集中在局部區段」，並提示看主圖差值帶找區段。
  **⚠️ 兩個「中位數」不是同一個東西（2026-08-13 實例）**：判讀句用
  **逐段差值的中位數** `median(後ᵢ−前ᵢ)`，摘要表用**各自中位數之差**
  `median(後)−median(前)`。**兩者可以同時正確卻相差 15.8 dB**——當
  差異集中在部分里程時（前趟早降、後趟晚降），逐段差值中位數為 0、
  整體中位數差卻很大。原規則只看 Δp5，遇上「兩趟都觸底所以 Δp5=0」
  就漏判，畫面上出現「無顯著變化」與「Δ P50 +15.8」並列。
  **判讀句須標明統計量**（「逐段差值中位數」而非「ΔSINR 中位數」），
  避免與摘要表同名不同義。
  **為何 Δp5 這次沒攔到（值得記的一般教訓）**：兩趟最差都觸到 -15 dB
  （模擬下限），**尾部被地板夾住就失去鑑別力**——**任何會飽和的指標
  都不能當唯一守門條件**。故改為「**摘要表任一欄顯著即觸發**」：
  守門條件與呈現內容綁定，**日後摘要表增欄位時必須同步加進判斷**
  （此對應關係已在程式碼註解標明）。
  **這類 bug 的特徵**：兩個數字都正確，所以任何斷言檢查都抓不到——
  **只有把它們放在同一畫面上才會現形**。

  **空態必須由資料明說，不能靠「畫面上沒東西」表達**（2026-08-13）：
  熱區**渲染失敗**與**兩趟無共同格**在畫面上完全同形——都是一片空白。
  此時**失效會冒充誠實空態**，而誠實空態正是我們刻意設計的
  （§0.2b 同一族：不得互相冒充）。故熱區在 `cells` 為空時要有明說文字；
  若文字說有格而圖上無格，那是**渲染故障而非資料狀態**，兩者不可再共用
  同一個畫面外觀。
  實例（2026-08-13 實測）：切換架次後熱區整層消失，**摘要與判讀句照常
  顯示同一組數字**——使用者只會以為「這兩趟沒得比」，不會懷疑畫面壞了。
  **驗收啟示**：熱區的空白**不能當作通過條件**，必須用注入已知必有格的
  資料去量彩色像素，否則測到的「符合預期的空白」與故障無法區分。

  **推廣到所有「空態即合法」的呈現，排序依據不是實作難度，而是
  「那個空態宣告的『沒事』有多接近『安全』」**：
  1. **事件流「尚無事件」** — 最高。它宣告的是**沒有異常發生**，
     靜默失效會讓故障中的飛機看起來健康；**空事件流是最強的安心宣告，
     也因此是最危險的謊**。
  2. **分級軌跡（無 SINR 欄就不畫）** — 次之。宣告「沒有量到訊號」，
     誤導研究判讀，但不涉飛安。
  3. **就緒三態的灰空心** — 最低。它宣告的是**資料不足**，
     渲染失效恰好落在保守側，**失效方向本身是安全的**。
  凡失效會落在「比真實狀況更令人安心」那一側的，排前面。
  **通則：同頁的自動判讀與數字表若可能互相矛盾，判讀句必須涵蓋數字
  表能揭露的情況**——否則使用者面對兩個都對的矛盾說法，只能靠猜。
  判讀句的責任不是「說出一個對的結論」，是「說出一個**不會與同頁其他
  證據衝突**的結論」。

### 6b.4 誠實規則（沿用全站）

- **樣本數必附**（摘要表獨立一欄、熱區每格 hover）。
- **無共同里程區間＝不畫空圖**（前端補位、核可）：兩趟若無重疊里程，
  直接說明「這兩趟沒有共同的里程區間——可能其中一趟未沿此路徑飛行」
  ＋附兩趟樣本數與分箱寬度；重疊 < 3 格時提示「趨勢不足採信」。
- **分箱寬度依樣本密度自適應、實際值標於卡頭**（前端補位、核可）：
  稀疏架次用固定 10m 格會導致兩趟永不落同格——**那不是「沒有差異」
  而是格子太細**，會把方法產物誤報成研究結論。
- 兩趟樣本數差距過大時（>3×）於摘要表加註「樣本數差異大，比較僅供
  參考」——**不阻止比較，但不假裝對等**。
- 分級不插值、未量測不上色、CVD 驗證照舊。

### 6b.5 資料契約（與後端對齊）

沿用 019 `query_signal_map` 架構，新增前後兩趟的對照查詢：
`{path_id, session_a, session_b, grid_size}` →
`{chainage[{m, a_sinr, b_sinr, a_rsrp, b_rsrp, a_n, b_n}],
  cells[{lat, lon, delta, a_n, b_n}], summary{a:{mean,p50,p5,n}, b:{…}}}`。
**弧長投影在後端做**（與 query_signal_map 同層，MCP 工具亦可用）。

## 6c. 資訊頁（/info）——2026-09-07 使用者指示，已實作

> 使用者原話：**「資訊頁要能在網頁端就看到所有歷史資訊」**（並指定：
> 把原本的「錄製」頁擴充成資訊頁；範圍含架次紀錄、事件歷史、任務執行紀錄、
> 錄製與檔案）

### 6c.1 為什麼要有這一頁

在這之前歷史散在三處：機隊頁有**每台機**的架次表、錄製頁有檔案、
場域訊號頁有訊號圖；而**事件與指令在網頁上根本沒有歷史檢視**——要查得進
資料庫下 SQL。於是「上禮拜那趟到底發生什麼事」這種問題，答案在系統裡卻不在
畫面上，而它正是飛完之後最常問的一句。

原「錄製」頁（issues/014）的定位是「這趟飛的資料我拿得到嗎」——**檔案只是
歷史的一種**。擴充成資訊頁之後，架次、事件、指令、檔案同住一頁；
`/captures` 保留 307 轉址到 `/info?tab=captures`（舊連結不讓它死掉）。

### 6c.2 三個分頁，順序＝回想一件事的順序

| 分頁 | 回答的問題 | 骨架 |
|---|---|---|
| **架次** | 那一趟發生了什麼？ | 一趟飛行 |
| **事件** | 那件事是什麼時候開始的？ | 時間（跨架次） |
| **錄製與回傳** | 那些資料我拿得到嗎？ | 檔案 |

分頁選擇進網址（`?tab=`）：重整不跳回第一頁，也貼得出連結。

### 6c.3 架次分頁＝左清單／右詳情（**不是四張並排的表**）

**一趟飛行是這個系統記憶的骨架**：事件、指令、錄製檔案全都掛在它下面。
四張並排的表會逼讀的人自己在腦子裡對時間軸——而那正是我方該替他做的事。

```
┌ 架次 29 趟 ──┐ ┌ pi5-… · 2026/9/2 16:34 · 37 秒 ────────────┐
│ 機 ▾ ☐含測試 │ │ 任務 / 鏈路樣本 / SINR / 高度 / 結束方式    │
│ 09/02 16:34  │ │ [▶ 開回放] [⤓ 匯出完整 JSON]               │
│ 09/02 16:18 1│ ├ 指令（含被擋下來的）──────────────────────┤
│ 08/13 16:37 8│ │ 16:34:12 上傳任務   ● 已執行               │
│ …            │ │ 16:34:31 返航       ● 守門擋下             │
│              │ ├ 事件 [全部｜機上訊息｜系統]────────────────┤
│              │ │ 16:34:40 ⚠ …（點列開詳情 modal §2.7）      │
│              │ ├ 錄製涵蓋（兩層覆蓋帶，與錄製分頁同一元件）─┤
└──────────────┘ └────────────────────────────────────────────┘
```

右側四段的順序＝回想一趟飛行的順序：**這趟是什麼 → 我下了什麼 →
發生了什麼 → 錄到的還在不在**。

* **清單上就看得出哪一趟出過事**：列尾徽章＝該架次的危急／警告事件數
  （`/api/sessions?with_events=true`）。沒有徽章＝沒有那種事件——
  **形狀先於顏色**，有徽章本身就是訊號。
* **「上鎖」與「我們看不到它了」分開講**：`end_reason` 逐值翻譯，
  `telemetry_lost` 寫成「遙測中斷（不代表飛行結束）」——兩者混為一談，
  事後看架次會以為那趟就是那麼長。
* **指令要含被擋下來的**（command_log 自 2026-09-06 起掛得上架次）：
  「我按了但它沒動」是事後最需要回答的問題之一。而**「守門擋下」與
  「送出後失敗」不同色**——前者飛機沒動，後者可能動了一半。
* 沒有指令時說「這一趟沒有從本系統下過指令」，**不說「沒有人操作過」**：
  飛手拿實體遙控器飛的那些，這張表本來就看不到。

### 6c.4 事件分頁為什麼要獨立存在

**實測資料庫裡兩萬則事件，掛得上架次的不到一百則。** 大部分事情發生在
還沒解鎖的時候——預檢擋下、遙控器離線、sysid 撞號、代理上線。那些正是
事後最想回頭查的，而它們不屬於任何一趟飛行。只做「這一趟的事件」等於
把最常查的那些藏起來。

* 篩選：機／嚴重度／來源／型別／全文搜尋。**型別下拉照資料庫實際有的列**
  （`/api/event-types`），不寫死清單——韌體與後端會一直長出新型別，
  硬編一份等於每加一種就多一個查不到的死角。
* 往回翻**用游標（`before_id`）不用 offset**：事件持續在寫，offset 分頁會在
  新事件進來時把同一則推到下一頁（或整則跳過）。
* 沒有機名的事件**留白，不寫「未知機」**——多數系統事件本來就不屬於
  任何一台機，替它掛一個「未知」是無中生有。
* 空結果分兩句：有篩選＝「這組篩選沒有符合的事件」、沒篩選＝
  「資料庫裡還沒有任何事件」。取得失敗走 `form-err`，**永遠不與空態同形**。

### 6c.5 事件文案：型別一律要有人話

歷史檢視一上線，`mission_shown`／`intent_*`／`rc_link`／`failsafe`／
`sysid_claimed`／`vehicle_ack`／`driver_disagreement` 這些型別就成了畫面上
最常出現的那幾種——而它們原本全部落到 `evText` 的 `default`，也就是
**一頁的 JSON 傾印**。已逐型別補上人話（`lib/evtext.ts`）。

認不得的型別：**先找它自己帶的那句話**（`text`／`reason`／`note` 是後端寫
事件時的慣例欄位，多半已經是整句中文），再退回傾印；**型別代號照留在
前面**——不知道那是什麼事件時，代號是唯一的線索。

配套 §0.3c：後端與代理的文案用 `**` 當強調記號，而畫面不解析 Markdown。
歷史資料改不了（那些字串已經寫進 `events.detail`），所以在**讀的這一端**
把記號畫成粗體（`lib/emph.tsx`）；`title`／SVG `<title>` 這類只吃字串的
位置用 `unemph()` 拿掉記號，不印星號給使用者看。

### 6c.6 資料契約（後端 2026-09-07 新增）

| 端點 | 用途 |
|---|---|
| `GET /api/sessions?with_events=&drone_id=` | 架次清單＋逐架次事件計數（計數**預設關**，只想列架次的呼叫端不付這個代價）|
| `GET /api/sessions/{id}` | 單一架次（深連結用）|
| `GET /api/sessions/{id}/commands` | 這一趟的指令（`/track` 也回，但它拖著整條遙測）|
| `GET /api/events?drone_id=&severity=&source=&type=&q=&since=&until=&before_id=` | 事件查詢；`session_id` 時間**正序**（讀一趟從頭讀到尾），其餘**倒序**|
| `GET /api/event-types?days=` | 出現過哪些型別（篩選下拉，不寫死）|

索引：`idx_events_session (session_id, time)`、`idx_events_drone_time
(drone_id, time DESC)`——原本只有 `idx_events_time`，問「這一趟發生了什麼」
得掃全表，而事件表是全系統寫得最兇的一張。

## 7. 待審決策點（請使用者指示）

1. 即時頁 HUD 是否需要加「飛行時間」（本次起飛累計）？消費機 app 常見，
   研究情境未必需要——預設不加，要就說。
2. 機隊頁「飛行中」chip 保留在收合行（安全狀態不藏）——同意嗎？
3. 回放頁播放速度選項（1×/4×/8×）收在 ⋯ 內——夠用嗎？
4. 你說的「有瑕疵」——若指本文件未涵蓋的具體問題，請直接指出，
   我修進對應章節。
