# UI 版本行為時間軸 · 2026-08-13（事故調查用）

用途：給定事故時刻，回答「當時操作者畫面**會顯示什麼／不會顯示什麼**」。
純由 commit 歷史推導，未接觸任何執行中服務。

⚠️ **這是「repo 時間軸」不是「操作者畫面時間軸」，兩者之間還有一步。**
前端 dev server 熱重載＝改檔即重編譯，但**哪一份程式碼實際在操作者那個分頁上執行，
取決於 HMR 有沒有成功推送到該分頁**（連線中斷、Fast Refresh 失敗、分頁未重整都會偏離）。
故本表給的是**候選行為**，要確定操作者實際看到什麼，需另外確認該分頁的載入／更新歷程。

**部署次數**：今日 `apps/frontend` 共 **20 個 commit（12:36:07 → 17:33:50，全部由前端 session）**，
但**部署事件是「存檔」不是「commit」**——commit 之間反覆存檔測試，
實際重編譯次數**明顯高於 20**。調查請記「**至少 20 次，實際更多，區間 12:36–17:33**」。
最後一次存檔 17:33:50，PM 通報後未再動。

## ⚠️ 全站不可用區間（最高優先，前端 session 提供）

**14:06:40（`b7b84ef`）～ 14:22:31（`3f39398`）之間，曾有約兩分鐘前端完全不可用**：
import 了 `@deck.gl/mesh-layers` 而容器 named volume 無該套件，webpack 一編即**整站 500**。
**若事故時刻落在此區間，操作者當下不是「畫面重載」而是「前端整個開不起來」**——
等級完全不同的事實。精確分鐘需前端 session 或容器日誌佐證。

## ⚠️ 任務疊圖：**兩段連續但相反的缺陷**（跨 08-11～08-13，非僅今日）

觸發條件（兩段皆同）：**須有啟用中任務且航點 ≥2**。手動飛行／無 active 任務不受影響。
事故描述為「任務執行中停止」，**條件正中**。

| 區間 | 行為 | 操作者後果 |
|---|---|---|
| **08-11 14:29 `c7c1394` ～ 08-11 17:35 `227fa64`** | 遷移後 plan 疊圖沿用 `beforeId:"path3d"`，該圖層已不存在 → **第一個 addLayer 即拋錯**，被 `catch { /* 無任務即無疊圖 */ }` 吞掉；後續兩層與 `setHasMission(true)` **全部不執行** | **計畫路徑完全不顯示**——看不到任務航線；deck 內容正常 |
| **08-11 17:35 `227fa64` ～ 08-13 13:08:47 `b8e5933`** | plan 疊圖恢復繪製，但建立在 overlay **之後** → interleaved overlay 被整個蓋住 | **實測軌跡、機體圖示、文字標籤全部被灰色計畫路徑蓋住**；量測：分級色命中 0 像素 |

**第二段的性質**：畫面**不會顯得壞掉**（灰色計畫路徑仍在），所以**失效冒充正常畫面**——
操作者在任務進行中看不到機在哪、也看不到軌跡，但沒有任何跡象提示畫面有問題。

**逐版查證（本 session 獨立驗，非轉述）**——**可覆核判準（照抄即可重現）**：

```bash
git show <commit>:apps/frontend/components/MapView.tsx | grep -c 'id: "path3d"'   # 定義，全窗口皆 0
git show <commit>:apps/frontend/components/MapView.tsx | grep -c '}, "path3d")'   # 位置引數
```
`c7c1394`(08-11 14:29)…`032358d`(16:36) 共 11 版：**定義 0、位置引數 1** → plan 首個
`addLayer` 對不存在的 beforeId 拋錯，被 `catch { /* 無任務即無疊圖 */ }` 靜默吞掉。
`227fa64`(17:35) 起：**位置引數 0** → 恢復繪製。**中間無空檔，兩段直接相接。**

⚠️ **不要用字串 `beforeId` 覆核**：那幾版是**位置引數**寫法（`addLayer({...}, "path3d")`），
`grep beforeId` 在破損版回 **0**、在已修的 `227fa64` 回 **1**（那 1 是註解）——
**與真相剛好相反**。我先前給 PM 的標籤即誤寫為「beforeId 使用」，數字對、標籤錯，
**照該標籤覆核會推翻一個正確的結論**。

**段一的完整症狀**（因拋錯發生在第一個 addLayer，`setHasMission(true)` 亦未執行）：
**地圖上沒有任務航線，且圖例不出現「預計任務路徑」那一行**——
與段二「看得到航線但看不到機」是**完全不同的抱怨形狀**。

> 對前端 session 提供之「窗口 47 小時」的更正：**覆蓋期實為 08-11 17:35 起約 43.6 小時**；
> 其前 3 小時是**另一個方向的缺陷**（完全不畫），不是同一件事。

## 即時監控頁（操作者飛行中看的畫面）

| 生效時間 | 此時間**之前**的行為（＝事故若早於此，操作者看到的） | 影響 |
|---|---|---|
| 12:52 `5c3a8e4` | 地面軌跡投影 1–3.5px（遠看等同消失）；機體圖示帶 heading 箭頭；圖示排序**未**高於軌跡層 | 中 |
| **13:08:47 `b8e5933`** | 見上方專節（**窗口回溯至 08-11，非僅今日**） | **高** |
| 13:13 `5b3e036` | 場域頁網格/起飛點圈蓋住累積軌跡與弱區輪廓 | 中（非即時頁） |
| 13:29 `14cf609` | **地圖初始中心寫死蘇黎世**；即時頁地面網格與起飛點圈錨死在 SITL 舊出生點（機隊已在台北大稻埕） | 高——空間定位參考錯誤 |
| 13:57 `c918a2f` | 側欄顯示「干擾區內」標籤（拿模擬器場景設定當現場事實斷言） | 中——可能誤導判讀 |
| 14:50 `33eabb9` | **機體標籤位置偏移單位錯誤**（+6 被當公尺）：俯視貼近、平視離很遠；同位標籤堆疊最遠 45px | 中——找機/辨識延遲 |
| 15:01 `1a07467` | **某縮放區間 2D 圖示與 3D 機體同時出現（重影）** | 中——「機在哪」判讀受損 |

## 事件流（本次調查關鍵）

| 生效時間 | 此時間**之前**的行為 | 影響 |
|---|---|---|
| **16:55:51 `4e3ae0b`** | **任一列 `detail` 非法 JSON → 整批拋出被靜默吞掉 → 畫面顯示「尚無事件」** | **最高** |
| **17:27:52 `9ae9dcc`** | **後端 5xx 不使 fetch reject，錯誤 body 是合法 JSON → 顯示「尚無事件」** | **最高** |
| 16:11–16:18 `7b1b5f1`/`6f2282b` | 事件無字典版本旗標；PX4 事件僅顯示代號無英文原文（014 字典 16:11 前未上線） | 中 |
| 17:06 `cda4a4f` | 壞列直接消失（非「無法解讀的訊息」） | 高 |

### 判讀邊界（已交 PM）
- **17:27:52 之前**：只要後端回 5xx，畫面就宣告「尚無事件」——與「確實沒有事件」**完全同形**，
  不可作為無事件之證據。（先前我報 16:55 有誤：那次只修「一列壞 JSON 吃掉整批」，
  5xx 那條晚了半小時才修。）
- **反向可信**：UI 有顯示的事件，必然有對應資料。
- 一律以 DB／後端紀錄為準。

## 回放／比較頁（事後判讀用，非飛行中）

| 生效時間 | 之前的行為 |
|---|---|
| 16:32 `09a0743` | 回放頁軌跡**間歇整條不見**（6 次載入踩到 2 次，競態） |
| 16:49 `4202915` | 比較頁切換架次後熱區整層空白 |
| 15:04 `af61305` | 比較頁判讀句與摘要表可互相矛盾 |

**若調查使用回放頁重建軌跡，須注意 16:32 前的版本可能整條不顯示**——
且該狀態與「此架次無軌跡資料」同形。

## 尚待確認（需 PM／後端補）
- 事故實際時刻與時區
- 同時段後端是否亦有部署
- 操作者當時使用的是即時頁、回放頁或 QGC
