# 實體驗證查核表（可直接填）

> 逐項填四格：**日期／韌體／結果／證據**。結果只有三種，沒有「大概可以」：
> `PASS`（判準那欄寫的，親眼看到了）、`FAIL`（沒看到或看到別的，照失敗處置做）、
> `SKIP`（沒測——**skip ≠ pass**，沒測就是沒有證據，不得往下走）。
>
> 「證據」要能讓別人重看：截圖檔名、`data/conformance/*.json`、tlog 時間戳、
> 或 `journalctl` 的那幾行。**寫「看起來正常」不算證據。**
>
> 設計背景與每一項的理由見 [`real-aircraft-verification.md`](real-aircraft-verification.md)。

## 受驗機體

| 欄位 | 值 |
|---|---|
| 機體名稱／`board_uid` | `pi5-ardu-quad` / `2a0020001151333139383538` |
| 飛控韌體 | `4.7.0 (official)` |
| 代理版本 | `0.3.0` |
| 場地與圍欄（半徑／高度） | |
| 執行人 | |
| 日期 | |

---

## 第 0 段 · 先讓地面站看得見（**其他全部建立在這之上**）

2026-08-26 11:15 起連續失去遙測近兩小時，期間操作員在飛、地面站在看凍結畫面。
**沒有這一段，下面每一項的「證據」都可能是舊資料。**

| # | 查核項 | 判準 | 失敗處置 | 日期 | 韌體 | 結果 | 證據 |
|---|---|---|---|---|---|---|---|
| **0.1** | 遙測不中斷 | 連續 10 分鐘 `/api/live` 的 `connected` 維持 true，且 tlog 持續增長 | 查 5G（`usb0` 是否 `Network is unreachable`）、查代理是否還活著 | | | | |
| **0.2** | 斷線看得出來 | 拔掉 5G → 畫面在 **10 秒內**明確顯示失聯，不是繼續顯示舊數字 | 目前只有 `connected:false`，數值仍是殘影——**凍結的數字比空白危險** | | | | |
| **0.3** | 幽靈 sysid 查清 | 連續 1 小時沒有非預期 sysid 自動建檔 | sysid 2／3 反覆出現，來源未查（飛控那條線上還有別的 MAVLink 裝置？） | | | | |

---

## 第 1 段 · 室內、機體上鎖、零風險

| # | 查核項 | 判準 | 失敗處置 | 日期 | 韌體 | 結果 | 證據 |
|---|---|---|---|---|---|---|---|
| **1.1a** | 機端身分有值 | `/api/live` 的 `flight_sw_version` 與 `board_uid` 都非 null | 查代理有沒有送 AUTOPILOT_VERSION | 08-26 | 4.7.0 | **PASS** | `4.7.0 (official)` / `2a0020001151333139383538` |
| **1.1b** | 板子 UID 跨重開機不變 | 飛控斷電再上電後 `board_uid` **字串完全相同** | FAIL＝不能拿它當機體識別，038 的自動建檔要改鍵 | | | | |
| **1.2** | 續飛機制在本韌體成立 | 上傳任務後 `POST /api/command/1/mission/goto {"index":1}` 回 `ok:true`，且 `mission_seq` == `wire_seq` | **FAIL＝停止**。飛行中改航線整個建立在它上面（4.0.3 拒指令 224、收訊息 41；4.7.0 未知） | | | | |
| **1.3a** | home 佔 seq 0 | `mission/upload` 回應的 `wire_items` ＝ 航點數 + 1 | 改驅動的 `home_at_seq0` 並重跑一致性測試 | 08-26 | 4.7.0 | **PASS** | 兩次上傳 4→5、7→8（`command_log` 10:12、10:13） |
| **1.3b** | 任務索引從 1 起算 | 執行任務時 `mission_seq` 從 **1** 起 | 同上。**FAIL 而沒發現的話，續飛會差一個航點且沒有錯誤訊息** | | | | |
| **1.4c** | failsafe 參數核定 | 逐項核對下表，與場地需求相符 | **一項都沒核定過**（issues/033）。不核定＝不知道機體自己會怎麼救自己 | | | | |
| **1.5** | 5G 訊號欄位對照 | 拿 **FM160** 原廠手冊覆核 `AT+GTCCINFO?` 欄位序 | 改 `modem.py` 的 `_parse_gtccinfo`。填錯**不報錯**，只讓干擾分析得到看似合理的錯答案 | | | | |

### 1.4c 逐項：要核的參數

> **前提：目前連讀都讀不到。** backend 連線時送 `PARAM_REQUEST_LIST`，但參數表
> 是空的——被 5G 斷線打斷後沒有重試。這一項要先修讀取才做得了。

| 參數 | 要確認什麼 | 現值 | 應為 |
|---|---|---|---|
| `BATT_LOW_VOLT` / `BATT_CRT_VOLT` | 低電壓門檻 | | |
| `BATT_FS_LOW_ACT` / `BATT_FS_CRT_ACT` | 低電量做什麼（0 無／1 Land／2 RTL／3 SmartRTL） | | |
| `FS_THR_ENABLE` / `FS_OPTIONS` | 遙控失聯處置 | | |
| **`FS_GCS_ENABLE`** | 開啟。飛控自己的 GCS 失聯處置＝第 1 層防線（2026-09-01 裁定，理由見表下） | | **1** |
| **`FS_GCS_TIMEOUT`** | **勾的是「關係成立」不是這個數字本身**：必須 **>「代理的 `LINK_LOSS_MAX_SOLO_S`」（現值 30s）**，否則飛控會搶在代理的分狀態處置之前動作，039 選項 C 每一格都不會發生 | | **45**（＝30＋裕度）|
| `FENCE_ENABLE` / `FENCE_RADIUS` / `FENCE_ALT_MAX` | 機端圍欄——**空中真正的防線**，不是我們的預檢 | | |
| `RTL_ALT` | 返航高度（地面下 RTL 會爬到這個高度） | | |
| `ARMING_CHECK` | **不要改**（使用者 2026-08-24 裁定）。記錄現值即可 | | 維持原值 |

> ### `FS_GCS_ENABLE` = 1、`FS_GCS_TIMEOUT` = 45（2026-09-01 裁定）
>
> **這一格的原始理由是錯的，先講清楚它錯在哪**，否則現場只會看到一個數字、
> 不知道為什麼不能改。
>
> 這一格原本寫「應為 0，因為飛控盯的是 `SYSID_MYGCS` 的心跳，而那個身分是
> **機上的代理**；5G 斷掉時代理照樣心跳，飛控完全無感」。**整句推論不成立。**
>
> 查證出來的事實鏈：
>
> 1. **心跳是地面發的，不是機上發的。** 指令服務每秒對每台機送一則
>    `MAV_TYPE_GCS` 心跳，`source_system = GCS_SYSID = 255`
>    （`apps/command/app/mav.py:189-193`）。
> 2. **機上代理從來不送心跳**，只收飛控的（機端 session 2026-08-31 於
>    `uav-agent` 查證：全 repo 沒有 `heartbeat_send`）。
> 3. **代理原封轉發地面站的封包**進序列埠（先解析驗 CRC 再寫）。
>
> 所以 sysid 255 的心跳**確實存在**，但它從地面出發、經 5G、經代理才到飛控。
> **5G 一斷，飛控那邊的 GCS 心跳就真的停了**——飛控不是「完全無感」，
> 它看得見。
>
> **這讓參數值變成一個真正的飛安取捨，而不是一個技術細節**：
>
> * `FS_GCS_ENABLE = 1`：真的多一層防線，而且完全獨立於 5G 與地面站。
>   **但它會搶在 [039](../issues/039-autonomous-flight-state-machine.md) 選項 C
>   前面**——ArduPilot 的 GCS failsafe 逾時遠短於我們的 30 秒單飛上限，
>   飛控會先動手，「失聯後跑完任務再 RTL」那條裁定實際上不會發生。
> * `FS_GCS_ENABLE = 0`：039 的分狀態處置照裁定生效，但**失聯時只有代理這一層**
>   ——代理自己掛掉（乙類失聯）時就沒有任何人了。
>
> 順帶一個相關情境：`= 1` 時，[034](../issues/034-healthz-hides-zombie-router.md)
> 的殭屍 router 會讓 5G 明明正常、飛控卻進 GCS failsafe——因為心跳的來源正是
> 那個停擺的迴圈。
>
> **2026-09-01 裁定：開（選項 B）。** 但當時寫的條件
> （`FS_GCS_TIMEOUT > LINK_ACTION_S` ＝ 10s）**太弱，已更正**：
> 代理在第 10 秒開始判斷，但 `FLYING_MISSION` 那一格會**繼續飛到單飛滿 30 秒**
> ——所以代理最晚動手的時刻是 30 秒，不是 10 秒。逾時設 15 秒的話飛控仍然會在
> 代理續飛的中途搶先。正確的不變式是：
>
>     FS_GCS_TIMEOUT  >  LINK_LOSS_MAX_SOLO_S（30s）  >  LINK_ACTION_S（10s）
>
> **代價要知道**：代理自己掛掉（Pi 死了、飛控還活著）時，**沒有任何人管到第
> 45 秒**；用出廠的 5 秒，那個情境 5 秒就有人接手。這組數字是在說**我們賭代理
> 活著的機率高於任務被無謂打斷的成本**——甲類失聯（5G 斷、Pi 與飛控都活）在
> 本場域是常態、乙類罕見，所以這個賭合理，但它是一個賭。要調整就調 45，
> 不必回頭改結構。
>
> **兩個數字分屬兩邊**（`FS_GCS_TIMEOUT` 在飛控、`LINK_LOSS_MAX_SOLO_S` 在
> 代理），**而分屬兩邊的關係正是最會漂移的那種**。所以這裡勾的是關係成立，
> 不是兩個各自的數字。更好的是讓代理開機時讀一次參數自己比對——它就在飛控
> 旁邊、手上又有自己的門檻值（已交辦機端）。

---

## 第 2 段 · 室外通電、不起飛

| # | 查核項 | 判準 | 失敗處置 | 日期 | 韌體 | 結果 | 證據 |
|---|---|---|---|---|---|---|---|
| **1.4a** | 遙控器預檢 | 遙控器開機綁定後，`PreArm: Radio failsafe on` **消失** | 先解決再往下——**遙控器是人接管的唯一途徑** | | | | |
| **1.4b** | 飛行就緒 | `/api/live` 的 `ready` 轉 `true` | 看 `not_ready_reasons` 逐項處理 | | | | |
| **2.0** | 解鎖不會自己動 | 在 `READY`（預檢全過）下解鎖，機體**維持 STABILIZE、不進入任何自動模式** | 2026-08-26 實際發生：在 RC failsafe 未解除時解鎖，六秒後被 failsafe 切進 LAND | | | | |

> **前置：換滿電電池。** 電池 failsafe 一旦生效，任何測試量到的都是 failsafe
> 的行為，不是系統的行為。

---

## 第 3 段 · 起飛（**依序，前一項沒 PASS 不做下一項**）

> 每一項都要有人握著遙控器、能隨時接管。場地圍欄先在 QGC 的 Plan 頁畫進 `.plan`。

| # | 查核項 | 判準 | 失敗處置 | 日期 | 韌體 | 結果 | 證據 |
|---|---|---|---|---|---|---|---|
| **2.1** | 解鎖與起飛 | `mission/fly` 到 15 m 並停住；讀回高度 ±2 m、模式 GUIDED/AUTO | 立刻接管降落。查 `takeoff_plan` 的高度語意（相對 vs 海拔） | | | | |
| **2.2** | 單點任務 | 兩點短航線能進 AUTO 並飛完，`mission_seq` 依序遞增 | 接管返航。查 1.3 的索引換算 | | | | |
| **2.3** | 中斷 | 飛行中 `mode/hold` → **讀回 `mode_verb == hold`**（不是看 ACK） | 接管。不成立則 2.6 不能做 | | | | |
| **2.4** | 返航 | `mode/rtl` → 自主返航、降落、上鎖 | 接管降落 | | | | |
| **2.5** | 續飛 | 飛行中 `mission/goto` 跳索引 → **目視確認真的改道** | 接管。1.2 過了但這裡 FAIL＝ACK 有效但行為不符 | | | | |
| **2.6** | 飛行中改航線 | 走 `mission/change-route`：確認畫面顯示的續飛航點、距離、方位與**實際飛的**一致；三步都讀回確認 | 接管。issues/039 的核心危害，真機上要親眼確認 | | | | |
| **2.7** | 地面站失聯 | 切斷 5G → 代理依選項 C 處置：任務中續飛至上限（**30 s**）或電量下限（**15 %**）才 RTL | **這一項尤其重要**：唯一「地面站看不到、只能靠機上」的防線。FAIL＝那條防線不存在 | | | | |
| **2.8** | 空中上傳被擋 | 飛任務中直接按「上傳」→ 收到 409 與三步說明，**機體不改道** | FAIL＝守門沒生效，狀態機文件 §3-A 的危害仍在 | | | | |

> **2.7 的兩個數字（30 s／15 %）是暫定值，本來就該由這次實測反推。**

---

## 完成簽核

| | 姓名 | 日期 | 備註（哪些是 SKIP、為什麼） |
|---|---|---|---|
| 執行人 | | | |
| 覆核人 | | | |

**沒有版本的驗證紀錄，等於沒有驗證紀錄。**
