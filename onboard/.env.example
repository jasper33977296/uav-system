# 機上節點設定：cp .env.example .env 後改成實際值。
# 腳本自動讀同目錄的 .env（真環境變數優先），systemd 與手動執行共用。

# 地面站 API（必填）——把 IP 換成你的地面站固定 IP。
# 現場部署逐項見主 repo 的 doc/deploy-checklist.md；機端 MAVLink 設定
# （同一個地面站 IP）見 rb5-setup/。兩邊必須填同一個位址。
GROUND_API=http://<地面站IP>:38000

# ── 以下皆選填（值為預設） ────────────────────────────────
# modem AT 埠（Quectel RM50xQ 通常是 /dev/ttyUSB2）
#AT_PORT=/dev/ttyUSB2
#AT_BAUD=115200

# 採樣頻率（AT 查詢延遲 100–500ms，1Hz 是務實上限）
#SAMPLE_HZ=1.0

# 持久緩衝（放斷電保留的分割區）
#BUFFER_PATH=/var/lib/uav-link/buffer.sqlite3

# 機上 PX4 MAVLink（udpin:// 寫法也接受）
#PX4_URL=udpin:0.0.0.0:14540

# RTT 目標（預設取 GROUND_API 的主機）
#PING_HOST=

# ── 這台機的身分（多機部署必設其一）─────────────────────
# 單機部署：兩個都不填＝記在地面站「主機」名下。
# **多機部署必設**：不設的話每台的即時訊號都會被記到主機身上（靜默混料）。
#
# 建議用 MAV_SYSID（機上本來就逐台設好的 sysid，見 rb5-setup/）：
# 節點啟動時向地面站 GET /api/drones 比對解出 drone_id——drone_id 是地面站
# 的 UUID，要等該機首次 MAVLink 連上被自動註冊才存在，機上無從預先知道。
#MAV_SYSID=1
# 已知 UUID 時可直接指定（相容路徑）：
#DRONE_ID=
