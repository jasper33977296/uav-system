# 複製為 .env.local（scripts/dev-frontend.sh 會自動複製）
#
# API/WS 位址**預設不用設**：signal.ts 會從瀏覽器網址推導 host、換上 backend
# 的 port（38000）。同一台開 localhost、區網開 192.168.x.x:33000 都自動對應。
# 只有 backend 跑在「與 frontend 不同的主機」時才需要指定：
#
# NEXT_PUBLIC_API_URL=http://<backend-host>:38000
# NEXT_PUBLIC_WS_URL=ws://<backend-host>:38000/ws/telemetry
