# 2026-08-04 開發紀錄

密度極高的一天：關掉全部 9 個 issue、定案並實作機上量測回傳的地面站側、
前端從單頁監控長成完整平台、開發環境容器化。

## 上午～下午：問題修正與範圍釐清

- **9 個 issue 全數關閉**（#001–#009，詳見 issues/），關鍵包括：
  - #001 鏈路三態機（實飛驗證完整 ok→degraded→lost→degraded→ok 序列）
  - #004 armed gate（待機不入庫；11,007 筆待機資料只有 2 個不同緯度的教訓）
  - #009 SITL 無 TTY 把磁碟寫爆（4,888 MB/hr 實測 → 0）
- **範圍釐清**（使用者確認，寫進 architecture.md）：
  - 本系統＝地面站上的無人機管理平台，控制資料蒐集與記錄事實
  - 無人機＝一台 UE，機上跑 ROS；換手由 modem／網路側處理，不研究
  - **系統對干擾無先驗知識**：干擾區只是模擬器輸入，
    真機階段量測資料才揭露干擾在哪——場域物件因此從前端全面移除
  - QGC 是包在系統下的控制元件；backend 對 MAVLink **只讀不寫**（硬規則）

## 機上量測回傳（roadmap 5 的地面站側）

- 設計定案 doc/onboard-telemetry.md：量測通道就是被量測的通道 →
  機上持久緩衝＋斷點續傳是正確性前提；HTTP 雙通道（即時 fire-and-forget
  ＋批次冪等入庫）；session 用時間戳反查
- 硬體確認：RB5 + RM500Q-GL，AT+QENG 直接給 SINR，schema 欄位一一對應
- 地面站側實作完成並以 fake-onboard-node 實測：25 秒斷線 26 筆全補回、
  0 重複 0 空洞；simulated 模式 push 回 409 防雙寫入者

## 前端：從單頁監控到完整平台

- **版面**：頂欄導覽（即時監控／無人機／任務）、WS 移到 shell 層換頁不斷線、
  彈性縮放（clamp 側欄、860px 堆疊、dvh）、深色為預設
- **3D 化**：傾斜視角＋地面網格＋起飛點錨定；軌跡從柱牆演進為
  **懸浮絲帶**（浮在飛行高度的彩色平面，垂直起降段同樣上色）；
  回放頁加方向箭頭；無人機本體用 three.js **正圓球體**
  （多機結構：以 drone_id 為鍵，群飛時不需改動）
- **無人機頁**：生命週期管理（註冊／刪除含防護／架次統計表）
- **回放頁**（roadmap 2）：3D 絲帶全貌＋SINR/RTT 雙圖時間軸＋事件標記
  ＋scrubber 球體游標同步
- **任務**（roadmap 3）：GET /api/mission/current 從機上唯讀讀回；
  路徑管理頁（.plan 上傳／機上讀回入庫／顯示於即時頁切換）；
  即時頁疊圖：任務庫優先、機上讀回備援
- 目視驗證抓到三個 API 層測不到的 bug：WS 事件 type 欄位衝突、
  高密度點描邊蓋掉填色、中途開頁漏事件——全修

## 環境

- 前後端進 docker compose（掛原始碼＋reload），全服務 restart:
  unless-stopped；綁 0.0.0.0，區網 192.168.137.111 直連，
  API 位址由瀏覽器網址自動推導
- VM 曾重開機（發現 db/sitl 原本沒 restart policy 的教訓）

## 壓力測試（收尾）

complex-survey.plan：9 航點、三高度層（30/45/60m）、四次進出干擾區。
356 筆樣本、12 筆鏈路事件、SINR 時序四個凹谷與四次穿越一一對應，
回放頁三層高度的絲帶＋箭頭＋球體游標全部正確呈現。

## 未決／下一步

- 機上 ROS node 實作（等 RB5 到貨；fake-onboard-node 的緩衝邏輯可移植）
- 上機第一個驗證：AT+QENG 的 SINR 是否隨已知干擾下降（同時驗整條鏈路）
- 前端失聯顯示（link_age_s 已在廣播內，缺 UI 呈現）
- 事件 detail 以人話呈現（現為 JSON 直出截斷）
- 多機：backend 擴 dict[drone_id, LiveState]（前端球體層已就緒）

## 深夜加場：無人機群模擬

- swarm_sim.py：運動學僚機（沿航點等速，無物理），與主機共用**完全相同的
  資料管線**（LiveState／link 狀態機／insert_*／WS）——DB 資料形狀與真多機一致
- 鏈路狀態機參數化（transition(state, m)），主機與每台僚機各自持有狀態
- 前端 store 改多機：fleet/trails 以 drone_id 為鍵；球體與絲帶全機隊渲染；
  事件流帶機名標籤；主機廣播帶 primary 旗標（側欄鎖定 MAVLink 機，
  不被僚機搶先的訊息頂掉）
- 實測：主機飛 complex-survey ＋ 3 僚機（35/55/70m 三種幾何）同時飛，
  四條航線各自入庫（96/81/89 樣本、最高高度與路徑設計一致、各自掃到 -15 SINR）
- 已知邊界：這是資料管線的多機驗證；真多機（多 RB5、多 MAVLink 連線）
  仍需 backend 的 ingest 多實例化，屆時 swarm_sim 退役
