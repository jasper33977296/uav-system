# 057 · 代理的事件清單沒有人讀：18 處往裡面寫，0 處讀

- 狀態：**closed**（2026-09-22，選 A：接上去；uav-agent 與 uav-system 各一個 commit，見〈解決方式〉）
- 嚴重度：medium（該讓操作員看到的事沒有送出去；附帶一個慢性記憶體成長）
- 位置：`uav-agent/agent.py`（`Monitor.events` 宣告與 18 處 `append`）、
  `uav-agent/intent.py:141`（**會動的那條路**）、
  `apps/backend/app/main.py:417`（`/ws/agent` 收 `event`）
- 建立：2026-09-21

## 現象

`agent.py` 的 `Monitor` 有一個 `self.events = []`，全檔 **18 處**往裡面丟東西：

| kind | 次數 | 例子 |
|---|---|---|
| `gs_link` | 5 | 地面站失聯／恢復（中斷幾秒）|
| `intent` | 4 | 改航線已確認、`{action} → {mode}` |
| `fs_gcs` | 2 | 飛控失聯 failsafe 自檢結果 |
| `takeover` | 2 | **代理接管開始／結束，期間做過什麼** |
| `statustext` | 1 | 飛控的 STATUSTEXT |
| `rc_link` | 1 | 遙控器在不在 |
| `guard` | 1 | 守門擋下了什麼 |
| `gs_link_action` | 1 | 失聯處置實際送了什麼指令 |

**沒有任何一行程式碼讀它。** 全庫只有 `.append`，沒有消費者、沒有序列化、
沒有人清它：

```
$ grep -rn "events" *.py | grep -vE "events\.append|self\.events = \[\]"
（只剩宣告那一行）
```

## 原因

不是「忘了送」那麼單純——**代理其實有一條會動的事件管道**，只是這個清單沒接上去。

`intent.py:141` 把事件經意圖通道送給地面站：

```python
self._send(ws, dict(self._pending.pop(0), type="event"))
```

而後端明確收這個型別（`main.py:417` 的 `/ws/agent`）：

```
收 `hello`／`state`／`event`／`ack`，反方向送 `intent`／`decision`／…
accepts: ["state", "event", "ack"]
```

所以管道是通的、後端也在等。`_pending` 由意圖處理函式的回傳值餵——
**而 `mon.events` 是另一條平行的、長得一模一樣的死路。**

## 影響

### 1. 該讓操作員看到的事，沒有送出去

最刺眼的是 `takeover`。那段程式自己的註解寫著：

> **接管結束要交代做過什麼。**「我不在的時候發生了什麼」是操作員回來第一個
> 要問的問題，而唯一答得出來的是代理

而那個答案被 append 進一個沒有人讀的清單。`gs_link_action`（失聯處置實際送了
什麼指令）同理——這是飛安相關的事實，只活在機上的記憶體裡。

**目前這些只能靠 journald 撈**：它們多半同時有 `log.warning`，所以不是完全
消失，但要 ssh 上機器 grep 才找得到，地面站與事後報告都看不到。

### 2. 看起來有、實際沒有

下一個人看到那 18 處 `append` 會合理推論「事件有被收集起來送到某處」，
於是做新功能時照著丟一筆。**2026-09-21 做 050 需求 1 時就差點這樣**——
原本打算照周圍程式的樣子 `mon.events.append(...)`，查了才發現丟進去等於沒做，
最後只留 `log.error`。

### 3. 慢性記憶體成長

清單只增不減。09-16 那天地面站鏈路每 30 秒抖一次（`gs_link` 一次失聯一次恢復），
光那一項就是每分鐘兩筆、每天約三千筆。代理是機上長跑的行程，不該有這種東西。

## 修法建議

先決定**要它還是不要它**，兩個方向都成立，不要留著現狀：

### A. 接上去（建議）

把 `mon.events` 接進既有的那條路——意圖通道的 `type: "event"`，後端已經在收。
要處理的：

- **節流與去重**。`gs_link` 在抖動的場域會狂噴，而 5G 抖動在本場域是常態
  （見 002 handover 事件抖動那次，最後整個型別被移除）。
- **斷線期間怎麼辦**：事件產生的時候往往正是送不出去的時候。要嘛沿用補傳
  （`Backfill`）那套緩衝＋確認的機制，要嘛明確決定「掉了就掉了」——
  但要決定，不要預設。
- 緩衝**必須有上限**，否則只是把現在的無限成長換個地方放。

### B. 刪掉

如果這些事 journald 就夠了，就把 18 處 `append` 與宣告一起刪乾淨。
**留著比刪掉糟**：它會持續誤導人，而且持續長大。

不論選哪個，順手把「事件要送到哪裡」寫進 `uav-agent/README.md`，
否則下一個人還是會重新發明一次這個清單。

## 解決方式

**選 A：接上去**，但**不是接進 `type: "event"`**——那是對 intent 的判決，後端會把它
寫成 `intent_{event}`，監視器的事件進去只會變成一串 `intent_None`。另開 `notice`
型別，規格在 `doc/agent-intent-protocol.md` §4.7。

### 18 處怎麼分

先問「地面站自己看不看得到」：

| 處理 | 處數 | 哪些 |
|---|---|---|
| **送**（`outbox.note`）| 11 | `gs_link` ×5、`takeover` ×2、`gs_link_action`、`fs_gcs` ×2、改航線序列逾時 |
| **刪**（只留 `log`）| 7 | `statustext`、`rc_link`、`_change`（armed／mode／電量門檻／GPS）——地面站 `mavlink_rx` 自己產生；`guard` 與 `intent` ×3——已經走 intent 的 `event` 回應進事件流 |

`Monitor.events` 宣告一起刪掉。**重啟觸發條件**：如果有一天地面站不再收
MAVLink 轉發（例如只靠代理的正規化 state），被刪的那 7 處就要改成送。

### 三個「要決定」的答案（實作時定）

* **斷線期間：緩衝，不是掉了就掉了。** 理由就在本案的清單裡——最重要的那幾則
  （地面失聯、接管、失聯處置）**正是在送不出去的時候產生的**。「掉了就掉了」
  等於這整件事白做。
* **送達：地面站逐則回執才刪**，沒回執的重連後重送（至少一次）；地面站以
  `boot`＋`seq` 去重、以缺號偵測丟失。不能只靠 TCP：失聯那一刻意圖通道的
  TCP 往往還沒被判死（`SILENCE_S` 10 s ＞ 失聯告警 5 s），`send()` 成功不代表送到。
* **上限 500 則**，滿了丟最舊的，`dropped` 進狀態列、缺號進地面站的事件。
* **節流只對 `gs_link`**：10 分鐘內前 4 則照送，其餘在窗口到期時彙總成一則
  「另有 N 則沒有逐則送出——現況看狀態列」。**不是靜靜丟掉**。接管與失聯處置不節流。

### 實作時另外修掉的

* **事件時間用發生的時刻**：`db.insert_event` 多一個 `at`。原本一律用寫入時的
  `now()`，晚到的事件會被排在「恢復」之後，時間軸說反。晚 ≥ 5 s 在 detail 帶
  `late_s`，畫面寫「晚 N 分才送到」。
* **後端去重記帳在寫入之後**：第一版在寫入前就推進號碼，寫失敗 → 不回執 →
  代理重送 → 被當成重送吞掉，那一則兩邊都沒有。
* **寫入失敗不拖斷意圖通道**：原本 `ws_agent` 裡的例外會一路拋到外層把連線關掉。
* **來源標示**：資訊頁與即時頁 HUD 的來源只分「機上／系統」，代理的事件會被標成
  「系統」（地面站推導的）。加上「代理」一格與篩選。

### 驗證

| | 結果 |
|---|---|
| `tools/notice-flow.py`（uav-agent，新）| ✅ 27 項：緩衝、回執才刪、別的行程的回執不算、重連重送、只送新的、上限與缺號、節流彙總、時間換算、舊版地面站不送 |
| 既有測試改接 outbox（`decision-flow`／`link-loss-table`／`fs-gcs-check`／`rc-link`）| ✅ 全過（開發機與 Pi 上都跑過）|
| 後端去重邏輯（容器內）| ✅ 首則／重送／寫失敗後重送／缺號 2／代理重啟 |
| 端到端（原始 WebSocket → 跑著的後端 → DB）| ✅ 95 秒前的事件記成 95 秒前、`late_s: 95`；重送只寫一列；缺號寫成 `gap_before: 2` |
| 前端（headless Chrome，資訊頁事件分頁）| ✅ 「代理接管……（晚 1 分才送到）」「……（這之前有 2 則沒收到）」，來源欄「代理」|
| 上機：部署後的代理 | ✅ `notice_ok: true`；059 A 的 `port_exclusive: true` 仍在 |
| 上機：Pi 經 5G 以測試身分送兩則 | ✅ 兩則回執；兩分鐘前那則時間正確（`late_s` 120），Pi 與地面站時鐘對齊 |

**沒驗到的**：真的失聯（5G 斷掉 → 代理記下 → 恢復後補送）沒有在機上重現。
Pi 上沒有 iptables／nft，能製造斷線的手段（route 黑洞）會連 SSH 一起斷，
不值得在一台接著飛控的機上冒這個險。邏輯由 `notice-flow.py` 的假 WebSocket
與端到端兩段分別覆蓋；**下一次場測有失聯時，看事件流裡有沒有 `agent_notice` 的
「地面站失聯」與「恢復」、時間對不對，就是這一格的驗收。**

### 解鎖的後續

059 B（搶埠報到地面站）與 050 需求 3（失聯期間持續出聲）現在都只要
`self.outbox.note(...)` 一行就送得到畫面上。
