# 071 · 無人機管理頁：「配置無人機參數」子頁

- 狀態：open（方向記錄，2026-09-22 使用者提出；未設計）
- 嚴重度：medium
- 位置：`apps/frontend/app/drones/`（無人機管理頁）＋指令服務 `set_params`
- 建立：2026-09-22

## 需求（2026-09-22 使用者）

未來在無人機管理頁會有一個**子頁面，專門配置無人機參數**。

起因：058 在真機第一輪抓到 `FS_GCS_ENABLE` 1→0、`FENCE_ALT_MAX` 6→18（不是經由地面站改的）。
使用者裁定**現在先不改回**，參數要改的時候走這個子頁。

## 已經有的零件

* 寫入：指令服務 `POST /api/command/{sysid}/params`（白名單、範圍、解鎖中 409、逐個讀回）
* 讀取：`GET /api/command/{sysid}/params?names=…`
* 現況與歷史：058 A 的 `drone_params`（每個參數最後確認的值與時刻）＋ `param_changed` 事件
* 「我們主張的設定」：`uav-agent/set-fc-params.py` 的 `PLAN` 表（058 B 要把它變成後端讀得到的資料）

## 要設計的

* 顯示哪些：白名單 20 筆？還是整份 1244 筆可搜尋、白名單可改？
* 與 058 B 的關係：頁面上直接標出「與我們主張的設定不同」的那幾格
* 與 068（飛行中調整）的關係：飛行中能改的那一小撮要在同一頁、另外標示
* 每一次改動都要進事件流（058 會自動認成「經由地面站指令服務」）

## 相關

* 058（參數變更可見）、068（飛行中調整）、033（`FS_GCS_ENABLE` 的裁定）

## 解決方式

（closed 時補）
