# 040 · sysid 必須由系統指派，不是由機自報

- 狀態：**in-progress**（設計 [`doc/drone-admission-protocol.md`](../doc/drone-admission-protocol.md) 已交付；2026-09-02 使用者四條裁定收斂了原本的待裁，**剩兩條**；分五批實作）
- 嚴重度：**high**（資料歸屬的根，撞號時後果是靜默混料）
- 位置：跨 `apps/backend/app/mavlink_rx.py`（歸屬）、`drones` 表、uav-agent（落地）
- 建立：2026-09-02

## 起因：同一件事至少發生過兩次，都沒有人發現

2026-09-01 為了跑 026 B4-d 起了 PX4 SITL。它的 sysid 是 **1**，而真機
`pi5-sdmodelh7v2-ardu`（ArduPilot 4.7，board_uid `2a00…`）的 sysid 也是 1。
系統**直接把模擬器認領進那台真機的記錄**，log 裡只有一行 `info`：

```
sysid 1 → pi5-sdmodelh7v2-ardu（既有主機）
```

結果：**PX4 1.14.3 的 `vehicle_event` 寫進了一台 ArduPilot 真機的事件流**。
清理時才發現**最早的一筆來自 08-24**——同樣的事以前就發生過，總共
**46 筆**假事件躺在真機的記錄裡，沒有任何人察覺。

遙測沒有被污染，但**那是運氣不是防護**：issues/004 的修法讓未 armed 不入庫，
而 SITL 從頭到尾沒解鎖。armed 過的話，模擬器的軌跡會直接進真機的飛行記錄。

## 根因：sysid 是機端自報的，而它是我們唯一的歸屬鍵

- 歸屬、路由、指令定址全部靠 sysid。
- 而 sysid 由機端自己宣告，我們**照單全收**。
- [038](038-board-identity.md) 記錄了 `board_uid`（真正穩定的身分），
  但**只記錄、不比對**——所以身分鏈存在卻沒有接上。
- 更糟的一段：認領時會把 DB 裡的 `board_uid` **回填**到新機的狀態上，
  **等於我們替它答了身分**。

同一個病在 [016](016-rb5-platform-connectivity.md) 也記過：
voxl-mavlink-server < 1.4.12 把全部機重設為 sysid 1。

## 已做（2026-09-02，038 的比對半邊）：讓它看得見，不是根治

`mavlink_rx._identity_guard`：

| 訊號 | 處置 | 為什麼強度不同 |
|---|---|---|
| **廠牌不合**（記錄是 ArduPilot、來的是 PX4）| **硬擋**：`identity_ok=False`，遙測與事件都不再記入該筆記錄，發 `critical` 事件 | 一台機不會重開機之後換廠牌，沒有誤判空間 |
| **`board_uid` 不合** | 只發 `warn`、**不擋**，但**不覆蓋** DB 的期望值 | uid2 跨重開機／韌體升級的穩定性還沒實測（`set_board_uid` 原註解的顧慮仍成立）；但覆蓋會把唯一的期望值抹掉 |

實測：同一個情境重跑，污染從 46 筆變 **0 筆**。
測試 `scripts/test-identity-guard.py`（10 項，含反向驗證）。

**但這只是讓撞號看得見。** 兩台機仍然可以宣告同一個 sysid，而先到的那台佔住
記錄、後到的被擋在門外——**被擋的如果是真機，那就是真機飛不了**。

## 需求（使用者 2026-09-02）：sysid 由系統指派

## 待決

1. **依據什麼身分指派？** 只有 `board_uid` 是機端不能亂改的（038）。
   但它要**開口問**（`AUTOPILOT_VERSION`）才拿得到——而問到之前，機已經在
   用某個 sysid 送遙測了。**指派必然晚於第一個封包**，所以要定義那段空窗
   怎麼處理（暫掛未指派區？照收但不歸屬？）。
2. **怎麼落到機上？** `MAV_SYS_ID` 是飛控參數，設了要重開才生效。
   誰來設——**代理最適合**（它在飛控旁邊、且已經有參數讀寫路徑），
   但那代表**沒有代理的機無法被指派**（第三方的機永遠不會有代理）。
3. **指派不了的機怎麼辦？** 至少三類：第三方的機、還沒裝代理的機、
   **SITL**。
   > ⚠ **實測（2026-09-01）**：對 PX4 SITL 送 `PARAM_SET MAV_SYS_ID=9`，
   > 回讀確認是 9，送重開之後**又變回 1**——那個映像每次啟動都是原廠狀態，
   > 參數不持久。所以「指派」對開發環境的 SITL 天生無效，
   > 得另想辦法（改映像啟動參數、或讓 SITL 走一條不參與指派的路徑）。
4. **撞號當下要做什麼？** 現在是「先到的佔住、後到的被擋」。指派制度下應該
   反過來：**未經指派的 sysid 一律不得入列**，而不是比誰先到。
   但那會讓「還沒被指派過的新機」連不上——第一次連線的雞生蛋要解。
5. **`is_primary` / 「既有主機認領」那條路徑要一起重看**：2026-08-24 已經
   因為它出過一次事（一筆停用的舊記錄仍是主機，新機一開機就被認領進去）。

## 相依

- [038](038-board-identity.md)：`board_uid` 是唯一穩定的身分鏈，本案的地基。
  比對半邊已於 2026-09-02 實作（見上），指派半邊就是本案。
- [016](016-rb5-platform-connectivity.md)：voxl 的 sysid=1 bug 是同一個病的
  另一個來源。
- [026](026-autopilot-driver-abstraction.md) B4-d：本案是它的前置——
  B4-d 要接 SITL 與真機同時在線比對，撞號沒解掉就跑不了。

## 設計已交付（2026-09-02）

[`doc/drone-admission-protocol.md`](../doc/drone-admission-protocol.md)。
使用者的需求是兩句：**sysid 必須由系統指派**、**身分驗證完成前不可以指派任務
或嘗試控制無人機**。設計把它拆成一台機的**入列狀態機**（SEEN／IDENTIFYING／
ADMITTED／QUARANTINED／UNMANAGED）、一組配號規則、與四層守門落點。

**先劃清楚的一件事**：MAVLink 本身沒有認證，所以這是**識別不是認證**。
它防得了撞號與設定錯誤（本專案至今所有身分事故都是這一類），
**防不了蓄意偽造**。真正的認證只有 MAVLink 2 簽章那條路，本階段不做——
但這件事必須寫明，否則它會被當成一道其實不存在的安全防線。

**兩條不可妥協的原則**：

* **不指揮 ≠ 不顯示。** 未入列的機照樣畫在畫面上、標明未驗證。天上有一台認不
  出來的機，**看不見比不可控更危險**。
* **嚴格入列之所以可以接受，是因為它從不移除最後的退路**——被擋下的機仍然可以
  用實體遙控器飛（033 第 3 層）。**沒有那一層的話，這套規則就會變成「把一台在
  天上的機鎖死」，那時它就不該這樣設計。**

**最需要小心的一格是「飛行中失去驗證」**：不撤銷指揮權（把在天上的機交給沒有
人更糟），只擋新任務指派，**緊急動作永遠放行**。

## 2026-09-02 使用者四條裁定，把設計收斂掉一半

| # | 裁定 | 效果 |
|---|---|---|
| 1 | **要被本系統控制，機上一定要有代理**（不接受純飛控）| 身分的錨點從 `sysid` 換成代理通道上的板號。**順手消掉一個待決點**：沒有代理的機不可指揮，所以也不需要配號 |
| 2 | **識別的唯一鍵值是板號** | `sysid` 降級成純地址 |
| 3 | **註冊時發現 sysid 被佔用 → 地面站配新號給那台的代理** | **撞號從兩難變成事務**——不必再判斷「要隔離誰」 |
| 4 | **MAVLink 2 簽章要排** | 成為 A5，但先做設計 |

**裁定 3 特別值得記**：本文原本兩個版本都在爭論「先到先得 vs 兩台都隔離 vs
看誰的綁定連續」——而**那整個問題是因為我們把 sysid 當成身分才產生的**。
身分收斂到板號、地址撞了就換一個，兩難整個消失。

**使用者的一句追問也修掉一個實質錯誤**：「飛行中失去驗證是什麼意思，板子總不會
在天上被換掉吧」——原本 §5 把「另一個來源冒用號碼」寫成了「這台機失去驗證」，
照那個寫法，**一台在天上的機會因為地面有人起了模擬器而被關掉指揮權**。

**剩兩條待裁**：號碼回收政策（建議明確釋放而非自動）、
簽章要保護哪一段（只保護代理↔地面站的話 TLS 更便宜——**這是 A5 設計的第一個
問題，不是實作細節**）。

**A1＋A2 就能擋掉本檔記載的全部三次事故**，而 A3 才需要動到飛機。
**但 A2 會影響開發流程**：代理強制之後 SITL 要被指揮就得掛代理，而現在沒有——
那要排進 A2 一起做，否則 A2 上線那天所有 SITL 都會變成指不動。

## 進度

### A1 配號登錄（2026-09-02 完成）

* `drones.assigned_sysid` ＋ **partial unique index**（`WHERE assigned_sysid IS
  NOT NULL`）。**與 `mav_sysid` 刻意分成兩欄**：前者是「我們配給這塊板子的
  號碼」（決定），後者是「這台機現在自報的號碼」（觀察）——
  **合成一欄就再也分不出「它跑錯號碼了」這件事**。
* `db.allocate_sysid(board_uid, claimed)` 實作設計 §3.2 的四格查表，
  回傳 `keep`／`change` 與人話理由。
* 號碼池 1–254；**255 永不配出**（我方 GCS）。用盡時**丟出明確錯誤**並說出
  該怎麼辦（釋放退役機的號碼），不是靜默給一個重複的。
* **號碼不自動回收**：釋放必須是明確動作。一個剛釋放又立刻配給別台機的號碼，
  會讓歷史資料的歸屬無法回溯——**同一個號碼在時間軸上指過兩台飛機，
  而遙測只記得號碼**。
* 一次性回填：既有記錄同時有板號與觀察號碼的，那組配對是既成事實，直接登錄
  （實測：真機拿到 `assigned=1`；沒有板號的 `uav-s3` 不回填——我們不知道它是
  哪塊板）。**衝突的不自動改**，留給人處理。

驗證 `scripts/test-sysid-allocation.py`（要在 backend 容器內跑）：四格全覆蓋、
09-01 情境（第 4 格）、255 不配出、用盡要明說，加一條反向驗證——
**直接對資料庫寫入重複配號，唯一索引真的擋得住**。

**本批不接執法、不寫機端**：拿到 `change` 目前只用於顯示與留痕。
執法是 A2、下發是 A3。

## ⚠ A2 開工前的實測發現：**SITL 給不出板號，而它的替代值是一個常數**

2026-09-02 直接問 PX4 SITL 要 `AUTOPILOT_VERSION`：

```
uid2 raw    = 000000000000000000000000000000000000   ← 全 0
uid2 去補位  = （空）
uid (舊欄位) = 0x4954414c44494e4f                      ← ASCII "ITALDINO"
```

**`uid2` 全是 0**，所以 `mavlink_rx.py:387` 的退路生效，改用舊的 `uid` 欄位——
而那個值解出來是 **ASCII 字串 "ITALDINO"**。**一個看起來像常數的東西，
不是一塊板子的序號。**

### 為什麼這件事會把整個設計推倒

裁定是「**識別的唯一鍵值是板號**」。若每一台 PX4 SITL 都回同一個 `uid`，那麼在
新制度下**所有 SITL 都是同一架飛機**——`ensure_drone_by_board` 會把它們全部解析
成同一筆記錄。那比原本的 sysid 撞號**更嚴重**：sysid 撞號至少還會被 09-02 的
`_identity_guard` 抓到（廠牌不同），而板號相同的多台 SITL 連矛盾都不會產生，
它們會**安靜地合併成一台**。

013 的群組任務是用 3–4 台 SITL 驗的。這條路不修，多機開發環境會直接壞掉。

> **誠實的界線**：我只問過**一台** SITL。「每台都回同一個值」是從那個值長得像
> ASCII 常數推論的，**不是實測**。要確認得同時起兩台比對——但無論是否為常數，
> 下面那條修法都成立。

### 修法方向：**全 0 的 `uid2` 不該退回舊欄位假裝有身分**

`uid2` 全 0 的意思是「這塊板子沒有 uid2」，而舊的 `uid` 欄位在 SITL 上是常數。
把常數當成板號，等於**發明一個身分**——那正是本案要防的事的鏡像。

建議：**`uid2` 全 0 → 沒有板號**（不是退回 `uid`）。後果是 SITL 永遠註冊不了，
也就永遠不可指揮——**而那正是「代理強制」裁定下應有的結果**：SITL 要被指揮，
就得由代理提供一個**明確標記為合成**的板號，而不是讓系統從一個常數裡誤讀出身分。

**這條要使用者裁定**，因為它會讓現行的開發環境立刻失去指揮能力。

### A2 入列守門（2026-09-02 完成）

* **`GET /api/admission/{sysid}`**（backend）：回 `seen`／`identifying`／
  `admitted`／`quarantined`／`unmanaged`＋人話理由。
  **由 backend 回答而不是 command 自己判斷**——入列要看的三樣東西（板號、
  代理連線、配號登錄）全在這一側；command 只有 MAVLink router，
  它看得到號碼、看不到身分，**而號碼正是那個會撞、會被冒用的東西**。
* **`command/admission.py` ＋ `_require_capability` 加入列檢查**（8 個端點全覆蓋）。
  **入列排在能力檢查之前**：對一台身分不明的機談「它做不做得到」沒有意義。
* **前端事先標示**：`notAdmitted` 時整個指令區換成告示、所有鈕停用
  （ui-spec §0.2c 條款 6——前提可事前查證時，不要讓使用者用失敗去發現）。
  `null`（還沒問到）**不擋**，與「問到了、沒過」不同形。

**問不到 backend 時的紀律與 `guard_client` 刻意不同**：後者問不到就放行
（它是額外一層，讓它的故障變成飛行操作的故障是把可靠度往下拉）；
**入列不是額外一層，它是那個門**——問不到就放行，等於「把 backend 弄掛」變成
一條繞過入列的路。折衷是快取：**答過的答案在 backend 掛掉時仍可用**（重啟不會
讓飛行中的機失去指揮），**從來沒答過的機不放行**。

驗證 `scripts/test-admission-gate.py`（對跑著的真實堆疊）：沒有代理的機
6 個端點全部 403、訊息說得出下一步與「實體遙控器不受影響」、
**403 而不是 501**（證明入列排在能力之前），加反向驗證——
**真機（有代理、配號相符）判 `admitted` 且不被入列擋下**。

> 測試過程修掉一個測試自己的問題：`/takeoff` 一度回 422 不是 403，
> 因為我沒帶 body——FastAPI 在端點函式跑之前就擋下了。**那不是漏洞**
> （畸形請求本來就到不了飛機），但測到的是「畸形請求進不去」而不是
> 「入列擋下」，兩者都安全、只有後者是要驗的東西。

### A3 自動換號（2026-09-02 完成，兩側）

**地面站側**：`state.reassigning`（`{from, to, phase}`）原封鏡像；
`/api/admission/{sysid}` 新增 `reassigning` 狀態，**排在 `board_uid` 檢查之前**
——重開飛控期間代理收不到 `AUTOPILOT_VERSION`，若先判 `identifying`，
畫面會說「身分未定」而不是「換號中」，**那會讓一個我們主動發起的動作看起來
像故障**。

**機端側**（uav-agent `a874afe`）：hello 帶 `sysid`；收到 `change` 後
`PARAM_SET MAV_SYS_ID` → 讀回確認 → 重開飛控 → 用新號碼回來 → 重新註冊。
守門比其他地方都嚴，因為**這是代理唯一會重開飛控的動作**：只在地面
（`flight_state()` 三格）、一個行程只試一次（**反覆重開飛控比不換號更糟**）、
讀回確認才重開、逾時放棄並大聲說。

`reassigning` 那一格不能省：換號期間機會用舊號碼消失、用新號碼回來，
**沒有它那個消失就是一次失聯**，039 選項 C 會據此動作——我們會為了修好身分而
讓飛機以為地面站不見了。（重開的是飛控不是 Pi，所以意圖通道不會斷。）

> **一個沒有補的方言缺口**：參數**讀取**已走驅動（`decode_param`），
> **寫入沒有 `encode_param`**（PX4 把整數位元組原樣塞進 float32）。
> 沒有在機端自己補——`autopilot/` 是上游的逐字複製品，加東西會讓
> `driver-parity.py` 從此都是紅的，**而那支護欄的價值就在於它平常是綠的**。
> 要補請補在上游。在那之前**非 ArduPilot 機不要開自動換號**
> （`--no-sysid-auto-apply`）。

驗證 `uav-agent/tools/sysid-reassign.py`（17 項）：含「在天上不換」
「讀回不符不重開」「逾時放棄」「只試一次」，與兩條反向驗證（關掉開關完全不動、
號碼本來就對也不動）。`driver-parity.py` 仍全綠。

### A4 認領以板號為鍵（2026-09-02 完成）

`drone_for_sysid` 的第 2 步原本是「**主機**還沒認領 sysid → 認領給主機」。
2026-08-24 就是這條路出的事：一筆早已停用的舊記錄仍是主機、`mav_sysid` 空著，
新接上的機一開機就被認領進去，`/api/live` 顯示**別台機的名字**。

**問題不在那條規則寫錯，在它用錯了鍵**：`is_primary` 是「哪一台是主要顯示
對象」，它從來就不是身分。**拿一個排版設定去做身分判斷，遲早會認錯機。**

改用**配號登錄**（`assigned_sysid`，鍵是板號）：認領因此以板號為鍵，
只是繞了一層號碼——而那一層是我們自己配的，不是機端自報的。

**另一半：收養佔位記錄。** 一台機通常先用 MAVLink 出現
（`drone_for_sysid` 建 `uav-s{sysid}` 佔位），代理稍後才帶板號來註冊。
兩條路各建一筆的話，**同一台飛機會有兩筆記錄，而遙測在其中一筆、身分在另一筆**
——那比沒有身分更難查。所以 `ensure_drone_by_board` 拿代理自報的號碼去收養
那筆佔位記錄；**只收養沒有板號的**——有板號的那筆已經有身分，蓋掉等於把兩台機
併成一台（09-01 的 SITL 事件就是這種合併的鏡像）。

驗證 `scripts/test-claim-by-board.py`（11 項）。**整個測試跑在一個一定會回滾的
交易裡**：`is_primary` 有唯一索引，要測那條路就得先把現有主機讓開——
而真實記錄本來就不該被測試改到。回滾也讓「結尾的收拾在中途失敗時不會執行」
這個老問題消失。

## 機端已完成（原本要交辦給 uav-agent session，該 session 已結束，改由本 session 做）

1. ~~**`say_hello()` 的 body 加 `sysid`**~~ → 完成（`a874afe`）。
2. ~~**收到 `sysid_action: "change"` 之後真的換號**~~ → 完成（`a874afe`）：
   `PARAM_SET MAV_SYS_ID` → 讀回確認 → 重開飛控 → 用新號碼回來 → 再註冊一次拿到
   `keep`。四個注意事項：**只在地面做**（用現成的 `flight_state()` 擋，
   只在 `DISARMED`／`NOT_READY`／`READY` 執行）；**`REASSIGNING` 期間要回報**
   （重開的是飛控不是 Pi，意圖通道不會斷——**沒有這一格，換號動作本身會觸發
   039 選項 C**）；**設了要讀回確認**；**PX4 SITL 參數不持久**（實測：設 9、
   回讀 9、重開又變回 1）。
3. **SITL 掛代理的形狀**——見上方的板號問題，這是最硬的一格。

> 代理的 `say_hello()` **本來就是照「板號為鍵」寫的**（docstring：「鍵是
> board_uid 不是 sysid」「等拿到 board_uid 才送」），所以上述是延伸不是改方向。

### A5 MAVLink 2 簽章：設計已交付（2026-09-02），待裁範圍

[`doc/mavlink-signing-design.md`](../doc/mavlink-signing-design.md)。

**第一個問題不是「怎麼做」，是「保護哪一段」。** 三段鏈路裡曝露面在
②（代理↔地面站，走 5G），而 ② 有兩條通道、答案不一樣：

* **意圖通道（WebSocket）→ 用 TLS（`wss://`）**：成熟、只要憑證，
  而且順便給加密——**簽章不加密**。
* **MAVLink（UDP）→ 這才是簽章該用的地方**：UDP 上沒有 TLS。

把兩半綁在一起問，會得出一個過度昂貴的結論。

**① 先不簽**：只簽 ② 時代理是信任邊界，拿到 Pi 的人可以對飛控下任何指令——
**但 Pi 上本來就有金鑰，簽 ① 擋不住「拿到 Pi」，只擋得住「摸到機體但沒拿到
Pi」**，那是很窄的情境。防機體遺失該做的是撤銷。

**兩個這個系統特有的坑**：

1. **時戳撞上那顆沒有電池的 RTC**。簽章用 48 位元時戳防重放，接收端拒絕倒退的
   時戳；而機上 Pi 冷開機時系統時間從 1970 起算、靠走 5G 的 NTP 修正。
   代理在同步前送的封包帶 1970 的時戳，NTP 一校正時戳暴增 56 年——
   **之後所有正常時戳都低於接收端記住的最大值，全部被當成重放丟棄**。
   補傳那次的解法（記單調時鐘、送出時換算）**在這裡用不上**：簽章時戳的語意
   就是牆鐘。正解是**時鐘沒同步就不要開簽章**。
2. **代理原封轉發，所以簽章可以端到端**（不重新編碼）；但**代理自己產生的訊息**
   （失聯 RTL、參數讀寫、換號）要由它自己簽——兩種來源要分開講，否則會漏。

**最危險的失效模式：簽章不符是靜默丟棄。** 那正是本專案反覆吃虧的形狀
（`SYSID_MYGCS`、補傳的狀態碼、兩支永遠紅的測試）。所以**偵測必須與簽章同批
上線**：指紋自檢（交換雜湊前綴、不傳金鑰）、簽章失敗計數上報。

**三條待裁**見文件 §6，其中第 3 條是誠實話：今天在**私有 5G 網段**，
而至今所有身分事故都是撞號與設定錯誤、**沒有一次是攻擊**——
**A5-a 的 TLS 那半現在就有價值**（意圖通道現在是明文 `ws://`），
MAVLink 簽章那半的價值在換到電信商 APN 那一天。

### A5-b／A5-c 已完成（2026-09-02）：金鑰保管與指紋自檢

**這一批還沒有在簽任何東西**，措辭一律是「尚未啟用」而不是「已保護」——
設計 §1 的紀律：**不要讓它被當成一道其實不存在的防線**。

**為什麼偵測先做**：簽章不符是**靜默丟棄**。金鑰一旦不對，畫面上一切正常、
指令全部消失——那正是本專案反覆吃虧的形狀（`SYSID_MYGCS` 靜默丟棄
MANUAL_CONTROL、補傳的永久錯誤綁狀態碼導致判定從未生效、兩支永遠紅的測試）。
**所以偵測要先於啟用，不能反過來。**

* `apps/backend/app/signing.py`：金鑰保管（一機一把）、指紋、配發、撤銷。
  **金鑰永遠不出現在 log／API／事件裡**，只傳指紋（雜湊，不是前綴——
  前綴會洩漏金鑰的位元）。
* `hello` 交換 `signing_fp`，四種結果**刻意分開**：`not_enabled`（常態）／
  `agent_missing`（配發沒完成）／`ground_missing`（金鑰檔可能掉了）／
  `mismatch`。**混成「好／壞」會讓最重要的那格消失。**
* **沒有做成 API**：一個能配發金鑰的 HTTP 端點，就是一個能把金鑰要走的端點，
  而 backend 目前沒有任何認證。改用離線工具
  `scripts/issue-signing-key.py`（設計 §2.2 的雞生蛋只能這樣解），
  **金鑰只印出一次**。
* 金鑰放**獨立 volume**（`signkeys`，0600），不與位址表共用——
  一個是設定、一個是秘密，混在一起的下場是備份與除錯時一起被帶走。
  **不放映像**（會推到 registry）、**不放資料庫**（會被 dump）。
  目前只有 backend 掛：command 與 heartbeat 要到 A5-d 才需要，
  **在那之前不把秘密送進用不到它的容器**。

驗證 `scripts/test-signing-check.py`（17 項）：含「指紋不是金鑰前綴」、
四種結果分開、不覆蓋既有金鑰、檔案權限 0600，以及一條反向驗證——
**金鑰檔壞掉時不能當成「沒啟用」**（那等於把一道防線靜靜關掉）。

### ⛔ A5 其餘部分：**裁定不做**（2026-09-02）

使用者：「簽章相關的東西跳過不做」。**A5-a（TLS）與 A5-d（真的開簽章）都不做。**

**重啟觸發條件**（寫在 `doc/mavlink-signing-design.md` 開頭，任一成立就回來）：
離開私有 5G 網段（換電信商 APN）／出現第一次真的與身分有關的惡意接入／
意圖通道要對外開放。

**這個裁定的代價要明記**：入列協定 §0 那張表的右欄（蓄意偽造、竄改、重放）
**現在明確地沒有人在管**。那是知情的取捨，不是遺漏——至今所有身分事故都是
撞號與設定錯誤，沒有一次是攻擊。

已做的 A5-b／A5-c 留在程式裡但**沒有在保護任何東西**（所有機都是
`not_enabled`）。留著的理由：無害、有測試，而重啟這條線時最花時間的不是那段
程式，是設計裡查證過的事實。**要刪掉也完全合理。**

### ~~A5 剩下的~~（已裁定不做，保留備查）

* **A5-a 的 TLS 那半待你決定憑證策略**：backend 現在同一個埠也服務前端，
  改 https 會牽動瀏覽器信任與前端的 API 位址。
* **A5-d（真的開簽章）**：要機端也持有金鑰、`command` 與 `uav-heartbeat` 也拿到
  金鑰，並先處理設計 §4.1 那個坑——**時鐘沒同步就不要開簽章**
  （那顆沒有電池的 RTC 會讓時戳暴增 56 年，之後所有正常封包被當成重放丟棄）。

## 解決方式

（closed 時補）
